# CLAUDE_KOR.md

이 파일은 `CLAUDE.md`(영어 원문)를 협업자들이 읽기 편하도록 한국어로 옮긴 것입니다. **Claude Code는 이 파일을 읽지 않고 `CLAUDE.md` 영어 원문만 기준으로 동작합니다** — 이 파일은 순전히 사람이 보기 위한 용도라, `CLAUDE.md`가 바뀔 때마다 사람(또는 Claude가 그 변경을 만든 세션 안에서)이 직접 같이 갱신해야 합니다(자동 동기화 안 됨).

## 프로젝트 개요

Cintamani는 NAS 웹 서버(예: Synology Web Station, NGINX)에서 정적 파일로 서빙되는 브라우저 스네이크 게임입니다. 빌드 과정도, 번들러도, 패키지 매니저도 없습니다 — `index.html`이 진입점이며 그대로 서빙됩니다. 게임 로직은 네이티브 ES 모듈(`<script type="module">`)로 작성되어 있는데, 최신 브라우저라면 별도 툴 없이 바로 동작합니다.

재작성 이전의 V1 원본 구현(전역 `<script>` 태그 방식, 모듈 아님)은 이제 작업 트리에 없습니다 — 한동안 `V1/` 폴더에 참고/롤백용으로 보관돼 있었지만, 더 이상 쓸모가 없어져서 삭제했습니다. git에 커밋돼 있던 상태라 필요하면 히스토리에서 언제든 복구할 수 있습니다.

## 게임 기획 비전

2026-08-13에, 원래 사용자가 직접 정리해준 게임의 전체적인 방향성 글을 바탕으로 기록. 이건 **게임이 앞으로 어디로 향하는지에 대한 기획 틀이지, 지금 만들어진 것의 스펙이 아닙니다** — 나중에 어떤 요청이 들어왔을 때 "이게 앞으로의 큰 그림에 맞는 방향인가, 아니면 어긋나는가"를 판단하는 데 참고할 장기적 맥락으로 다루고, 이걸 그대로, 한 번에 전부 구현해야 할 백로그로 취급하지 마세요. 위 협업 방식 섹션에 따라, 아래 내용 중 무엇이든 실제로 구현하기 전에는 여전히 "기획 → 모듈 적합성 검토 → 쉬운 말로 설명 → 승인" 루프를 거쳐야 합니다. 아래 기획 요소 중 이미 아키텍처(더 아래 참고)의 무언가와 맞아떨어지는 부분은 명시적으로 표시해뒀고, 그 외 나머지는 **전부 아직 만들어지지 않은 것**입니다.

### 큰 구조: 이무기 → 용/뱀 여정

한 플레이 전체가 의도한 흐름은, 장소 단위로 보면 다음과 같습니다:
1. **캐릭터 선택** — 최종적으로 이시미, 훼룡, 율무기, 강철이 총 4종의 플레이 가능한 이무기 종류. 처음엔 하나만 가능하고 나머지는 나중에 해금. **아직 구현 안 됨** — 지금은 선택 가능한 정체성 없이 뱀 한 종류만 존재합니다. 나중에 이걸 만들 때는, 적/아이템/메커닉/여의주가 이미 쓰고 있는 "def 파일 + `index.js` 등록" 패턴(아래 아키텍처 참고)을 그대로 따르는 새 `content/characters/` 같은 레지스트리 카테고리로 만드는 게 자연스럽습니다 — 특수 케이스로 따로 분기 처리하는 방식이 아니라.
2. **시작 장소**(작은 연못/구덩이) — 여기서 시작해서, 길이나 처치 수 마일스톤을 달성하면 다음 장소로 이동 가능. 마일스톤으로 무언가를 해금하는 *메커니즘 자체*는 이미 축소판으로 존재합니다(`VENOM_UNLOCK_LENGTH`, `SCALE_WAVE_UNLOCK_CHASER_KILLS`, sentinel/turret의 `LONG_SNAKE_UNLOCK_LENGTH` — 아키텍처의 "두 가지 원거리 무기" / "적 종류" 참고). 하지만 **"장소/스테이지"라는 개념 자체가 지금은 전혀 없습니다** — 지금 게임은 하나로 이어진 필드, 하나로 이어진 세션일 뿐이고, 서로 구분되는 "장소" 사이의 전환이 없습니다.
3. **거북 사부**(용왕의 사자) — 다음 장소에서 만나 스킬/생존법을 배움. NPC와의 대화/교육 상호작용이 필요하다는 뜻인데, 아직 존재하지 않습니다. 아마 별도 시스템으로 만들기보다는, 아래의 미래 "비전투 스테이지" 모드가 어떤 형태로 결정되든 그 안에 들어갈 가능성이 큽니다.
4. **용/뱀 분기 + 서로 다른 용의 형태들** — 결과(용이 되어 승천하는지 뱀으로 남는지, 그리고 승천한다면 *어떤* 용의 모습/능력인지)는 마지막에 한 번 고르는 엔딩 플래그가 아니라, 플레이 내내 쌓인 선택들에서 자연스럽게 도출되어야 합니다. 이건 플레이 도중 어떤 형태로든 선택/"업보"를 계속 추적하는 상태가 필요하다는 뜻이고, 그 범위(한 판 안에서만 유효한지, 아니면 실제 저장이 필요한 수준인지)는 나중에 명시적으로 결정해야 합니다 — 지금 코드베이스는 세션 간에 어떤 상태도 저장하지 않습니다(`localStorage` 없음, 계정 없음).
5. **점점 강해지는 도전 + NPC 상호작용의 반복** — 더 강한 적/장애물을 극복하는 과정과, 다른 동물·사람의 요구에 응답해서 서로 다른 보상을 얻는 과정이 번갈아 반복. "새로운 적/장애물" 쪽은 이미 있는 `content/enemies/` + `content/mechanics/` 레지스트리(아키텍처 참고)가 정확히 이걸 위한 것이라 새 시스템이 필요 없고, 콘텐츠를 기획하는 대로 def 파일만 늘리면 됩니다. "상호작용하고 응답을 고르는" 쪽은 새로운 부분으로, 아직 없는 분기형 대화/선택 UI가 필요하다는 뜻입니다.
6. **최종 관문 → 승천 또는 뱀으로 남음** — 4번의 분기 엔딩으로 이어지는 클라이맥스 관문 하나. 아직 구현 안 됨.
7. **승천 이후의 엔드컨텐츠 또는 엔딩** — 아직 구현 안 됨. 반복 플레이 가능한 엔드컨텐츠로 갈지, 한 번 보고 끝나는 엔딩 연출로 갈지 범위도 아직 미정.

### 서사

구체적인 스토리는 아직 일부러 채워넣지 않은 상태입니다("향후에 구체적인 스토리를 녹여넣을 예정") — 그게 쓰이기 전까지는 코드와 맞춰볼 내용이 없습니다.

### 핵심 플레이 방식

아래 아키텍처와 대조해보면, 대부분은 이미 지금 빌드에 해당하는 내용입니다:
- **방향키 이동, 고정 틱 기반 자동 이동, 먹이로 성장, 자기 몸/벽 충돌 시 사망** — 지금 구현과 정확히 일치(`core/loop.js`의 고정 틱 모델, `entities/snake.js`의 성장 큐, 충돌 순서 섹션의 벽/자기 충돌 단계).
- **스페이스바 + ASDF, 총 5개의 기능 키 예산** — 지금은 스페이스바만 실제로 게임 동작(독침/비늘파동, "두 가지 원거리 무기" 섹션에 설명된 raw `keydown`/`keyup` 리스너 방식)에 연결돼 있고, A/S/D/F는 미사용입니다. 이건 기존 걸 재배치하는 게 아니라 **미래를 위해 미리 잡아둔 예산**으로 읽어야 합니다 — 새 스킬이 기획되면 A/S/D/F가 그 자리로 예약돼 있고, 지금은 다른 어떤 키 바인딩(P/Escape 일시정지, 타이틀의 Enter/`t`/`l`, 테스트 모드의 `1`/`2`/`3` 여의주 디버그 키)과도 겹치지 않습니다.
- **다양한 먹이 종류 + 적/장애물 + 투사체나 지형/적에 영향을 주는 스킬** — 이미 오늘 다 있는 내용입니다: `content/items/`에 기본 먹이 외 5종(speedUp, speedDown, tripleGrowth, attackUp), `content/enemies/`에 적 5종, 그리고 독침/비늘파동/여의주 스킬(붉은 광선, 푸른 효과)이 정확히 이 "투사체 또는 범위 스킬" 카테고리입니다. 여기엔 빈틈이 없습니다.
- **몸의 궤적에 따른 효과** — 적을 에워싸 죽이거나, 특정 몸 모양을 그려서 능력을 발동 — 이미 구현되어 있습니다: 똬리로 가두는 encirclement 캡처 메커닉(`content/mechanics/encirclement.js`)과 여의주의 패턴 매칭 발동 시스템(`algorithms/patternMatch.js`, 아키텍처의 여의주 섹션 참고)이 정확히 이겁니다.
- **자유 이동 "비전투 스테이지" 모드** — 특정 장소에서는 뱀을 틱에 묶이지 않고 플레이어 마음대로 움직여서 사람/동물과 상호작용할 수 있어야 함. **아직 구현 안 됨**, 그리고 작은 추가가 아닙니다: 지금 아키텍처 전체가 전제하는 "항상 자동으로 움직이는 틱 루프"와는 근본적으로 다른 이동/입력 모델입니다. 나중에 이걸 기획하게 되면, `playingState`에 모드 플래그를 덧붙이는 방식보다는 (이미 `testBuildSelect`/`leaderboardView`가 각자 자기 입력 바인딩을 가진 독립된 상태로 존재하는 것과 같은 정신으로 — 아키텍처의 "게임 상태 머신" 참고) 그 자체로 새로운 상태 머신 상태가 되는 쪽이 유력합니다.

## 협업 방식

이제(2026-08-03 기준) **개발 경험이 없는 협업자가 두 명**이고, 각자 이 저장소를 로컬에 따로 클론해서 자기 자신의 Claude Code 세션으로 작업합니다: 원래 사용자(게임업계 배경은 있지만 소프트웨어 개발 경험은 없는 게임 기획자)와 곧 합류할 친구(원래 사용자보다도 기술적 배경이 더 없음). 둘 중 누구의 요청이든 **그대로 구현할 스펙이 아니라 방향성/아이디어**로 다뤄야 합니다. 말한 그대로 문자 그대로 구현하면, 둘 다 예상할 수 없는 방식으로 기존 아키텍처와 충돌할 수 있습니다. 실제로 어떻게 구현할지에 대한 개발 주도권은 (개발 PM이자 엔지니어 역할을 겸하는 Claude에게) 있으며, 요청의 문구 그 자체에 있지 않습니다. 지금 대화 상대가 누구인지에 맞춰 설명 수준을 조절하세요 — 원래 사용자는 업계 배경 덕분에 좀 더 맥락을 받아들일 수 있고, 친구 쪽 세션은 더 쉬운 말과 더 작고 구체적인 단계에 훨씬 더 기대야 합니다.

사소하지 않은 요청(새 기능, 동작 변경, "이거 어떻게 생각해?" 같은 의견 요청)에는 코드를 작성하기 전에 다음 루프를 따르세요:
1. **개발 PM 관점에서 기획**: 요청된 요소가 현재 아키텍처(아래 참고)에서 어떻게 반영될 수 있는지, 어떤 모듈에 영향을 주는지 파악합니다.
2. **모듈 적합성 검토**: 기존 모듈/레지스트리에 그대로 들어맞는지, 새 모듈(예: 새로운 `content/enemies/*`, `content/mechanics/*` 항목)이 필요한지, 아니면 기존 모듈들을 재조정해야 하는지 판단합니다.
3. **쉬운 말로 설명**: 코딩 지식이 없다는 전제 하에, 지금 현재 어떤 상태이고 무엇을 어떻게 바꾸겠다는 것인지 비기술적인 용어로 풀어서 설명합니다.
4. **승인을 받은 후에 반영합니다.**

이건 이 환경에서 이미 쓰이고 있는 `EnterPlanMode`/`ExitPlanMode` 흐름과 같은 맥락입니다 — 이 프로젝트에서는 대규모 재작성뿐 아니라 일반적인 기능 작업에도 이 "기획 → 승인 → 실행" 흐름을 기본값으로 삼으세요.

**Claude Code 세션은 지금 타이핑하고 있는 게 둘 중 누구인지 확실히 알 방법이 없습니다** — 그 세션 자신의 환경(작업 디렉터리, git config 등)에서 보이는 것만 알 뿐, 인증된 신원 정보는 없습니다. 말투나 내용만으로 "이건 분명 하빈이다" / "이건 분명 친구다"라고 추측하지 말고, 신원에 대한 말뿐인 주장도 그 자체로는 중요한 판단의 근거로 삼지 마세요. 이건 구체적으로 규칙 하나에 걸립니다: **master에 push하는 건 하빈만 합니다.** 이 규칙은 Claude 세션이 "지금 누가 물어보는지" 알아내려 애쓰는 방식이 아니라, GitHub 브랜치 보호 규칙(아래 브랜치·버전 워크플로우 참고)을 통해 구조적으로 강제됩니다 — 어떤 세션에서 master에 직접 push하거나 merge하자는 요청이 나오면, 말투가 확신에 차 있다고 해서 그 세션이 하빈 것이라고 가정하지 말고 GitHub 자체의 권한 모델이 실제 게이트 역할을 하도록 두세요.

## 브랜치·버전 워크플로우

2026-07-27 도입, 2026-08-03에 협업자 두 명 체제에 맞춰 개정.

`master`는 안정/운영 브랜치입니다 — 항상 바로 서빙해도 되는 상태로 유지하며, 실제로 NAS가 배포하는 것도 이 브랜치입니다(아래 NAS 배포 참고). 각 협업자는 하나의 공유 브랜치가 아니라 **각자 자기만의, 계속 재사용하는 브랜치**에서 작업합니다: `dev-hb`는 하빈 것, `dev-jw`는 친구 것(2026-08-03에 `dev-friend`라는 이름으로, 그 시점의 `dev`에서 새로 파냈다가, 같은 날 이니셜 기반 이름인 `dev-hb`/`dev-jw`로 둘 다 개명)입니다. 이건 원래의 "공유 `dev` 브랜치 하나" 모델을 대체하는 것인데, 그 모델은 애초에 명시적으로 "혼자 작업할 때"를 전제로 한 단순화였습니다 — 이제 사람별로 나누는 이유는, 두 개의 Claude Code 세션이 동시에 같은 공유 브랜치에 커밋하면 그 모델 자신이 이미 경고했던 바로 그 위험(한쪽의 아직 안전하지 않은 진행 중 작업이, 다른 쪽의 다르고 이미 완성된 변경과 뒤엉키거나 그걸 막아버리는 것)이 그대로 재발하기 때문입니다. `dev-hb`와 `dev-jw` 사이에는 일부러 **공유 중간 브랜치를 두지 않습니다** — 각자 자기 pull request로 독립적으로 `master`에 merge하므로, 한쪽의 진행 중인 작업이 다른 쪽과 같은 브랜치에 걸쳐 있는 일이 절대 없습니다.

**브랜치별 작업 흐름**: 구현 + 헤드리스 검증(아래 커맨드 참고) → 브랜치를 GitHub에 push → `master`를 대상으로 pull request 열기 → diff 검토 → merge. 하빈 본인의 PR(또는 직접 push)은 예전처럼 가볍게 처리해도 됩니다 — 이 추가 절차는 친구의 변경사항을 위한 것으로, merge 전에 하빈이 검토한다는 게 핵심입니다.

**`master`에 걸린 GitHub 브랜치 보호 규칙**(2026-08-03에 `gh api`로 설정): 어떤 pull request든 merge하려면 승인 리뷰가 최소 1개 필요하고, GitHub는 PR을 올린 본인이 자기 PR을 승인하는 걸 허용하지 않습니다 — 그래서 `dev-jw`에서 올라온 PR은 친구 혼자서는 merge할 수 없고, 어느 세션에 뭐라고 시키든 상관없이 하빈의 리뷰가 필요합니다. `enforce_admins`는 **꺼둔** 상태라, 하빈(저장소 소유자)은 여전히 `master`에 직접 push하거나 리뷰를 기다리지 않고 merge할 수 있습니다 — 하빈 본인의 변경사항에 대해서는 기존 혼자 하던 방식 그대로 유지되는 것입니다. 참고로 남겨둘 사항: 이건 조직(organization) 소유가 아니라 개인 GitHub 저장소라서, 더 강력한 "특정 사용자만 push 가능하게 제한" 브랜치 보호 기능은 쓸 수 없습니다(GitHub가 그 기능을 조직 저장소 전용으로 제한함) — 지금 "친구 혼자서는 master에 코드를 못 넣게" 실제로 막아주는 건 필수 리뷰 게이트이지, 사용자별 push 허용목록이 아닙니다. 나중에 이 저장소가 조직 소유로 옮겨간다면 이 부분을 다시 보고, 훨씬 강력한 보장인 명시적 push 제한으로 바꾸는 걸 고려하세요.

**밸런스 실험을 위한 2단계 "실험실" 승격 방식**이 존재하는 이유는, 친구가 오직 *배포된* 빌드만 플레이할 수 있고 로컬 개발 버전은 절대 못 하기 때문입니다 — 그래서 아이디어 하나를 같이 테스트해보려면, 그게 "결정됐다"고 부르기 한참 전부터 이미 `master`에 push해야 합니다. 테스트 모드의 빌드 선택 시스템(더 아래 "테스트 모드 빌드 선택" 참고)이 바로 이걸 안전하게 만들어주는 장치입니다: `config/testBuilds.js` 뒤에 숨겨진 실험은 `master`에 실제로 배포된 뒤에도 일반 플레이에는 완전히 안 보이는데, 그 테스트 모드 화면을 통해서만 도달할 수 있기 때문입니다. 그래서 `master`로의 첫 merge는 배포된 NAS에서 테스트하는 누구에게나(정상 플레이어는 절대 못 봄) 매우 낮은 위험으로 실험 후보를 보여주는 셈이 되고, 그게 실제로 확인된 뒤에야 누군가 명시적으로 승격을 요청해서(예: "이 실험 실제 게임에 반영해줘") `testBuilds.js`에서 꺼내 진짜 기본 동작으로 만드는데, 이건 그 자체로 별도의 후속 변경/커밋/merge입니다.

위 내용을 제외하면, 릴리스 워크플로우의 나머지는 누구 브랜치에서 왔든 그대로입니다: 변경사항이 확정되면(헤드리스 검증 + 그 변경의 주인이 브라우저로 테스트) README.md의 변경 이력 표에 새 줄을 추가하고, merge 커밋에 태그(`git tag vX.Y.Z`)를 남겨서 그 시점 상태를 언제든 다시 보거나 되돌릴 수 있게 합니다. 기준점: `v2.1.0` 태그 = 이 워크플로우를 처음 도입하기 직전 상태.

**개인 브랜치를 (실제 GitHub PR이 아니라) 로컬에서 `master`에 합칠 때는 `git merge --ff-only`를 쓰고, `--no-ff`는 쓰지 않습니다.** 공유 중간 브랜치가 없고 개인 브랜치(`dev-hb`/`dev-jw`)는 항상 `master`보다 앞서있기만 한 구조라, 둘이 갈라져 있지만 않다면 fast-forward가 원래 "맞는" 결과입니다 — `--no-ff`는 필요 없는 머지 커밋을 억지로 만들어내고, 더 나쁘게는 그 머지 커밋이 `master`에만 존재하고 개인 브랜치엔 없어서, 그 이후의 모든 머지에서 fast-forward 관계를 조용히 깨버립니다. 실제로 이런 일이 있었습니다(2026-08-10): `dev-hb`에서 `master`로 `--no-ff` 머지를 연속 두 번 했더니 `master`와 `dev-hb`가 서로 갈라진 지점을 가리키게 됐고(어느 쪽도 다른 쪽의 조상이 아님), 그다음 `--ff-only` 시도가 `fatal: Not possible to fast-forward`로 실패했습니다. 해결은 `dev-hb`에 `git merge origin/master`를 한 번(반대 방향이 아니라 이 방향으로) 실행해서 `master`가 갖고 있던 내용을 `dev-hb`에 다시 합쳐 동기화한 것 — 그 뒤로는 `--ff-only`가 다시 정상 동작했습니다. 이미 만들어져 push/태그까지 된 과거 `--no-ff` 머지 커밋 두 개는 다시 쓰지 않고 그대로 뒀습니다(이미 push/태그된 히스토리는 재작성하지 않음) — 이건 어디까지나 그 이후부터 적용하는 수정입니다. 결과를 push할 때는 브랜치용 `git push` 한 번, 태그용 `git push` 한 번을 따로 하지 말고 `git push --follow-tags origin master`를 쓰세요 — 명령 하나로 끝나고, 이 워크플로우가 이미 쓰고 있는 **annotated** 태그(`git tag -a`)만 같이 딸려갑니다(가벼운/lightweight 태그는 안 됨).

**`BUILD_VERSION`(`config/constants.js`)과 `index.html`의 `?v=` 캐시 무효화 문자열은, master merge 시점뿐 아니라 브랜치에서 뭔가 바꿀 때마다 매번 같이 올립니다** — 누구 브랜치인지와 무관하게. `BUILD_VERSION`은 화면(캔버스 우측 하단, `render/layers.js`의 `versionLayer`)에 그대로 표시되는데, 이건 플레이어가 새로고침했을 때 최신 코드가 실제로 반영됐는지 화면만 보고 바로 확인할 수 있게 하기 위해서입니다 — 실제로 사용자가 시각적 변경이 반영됐는지 헷갈려한 일이 있어서 추가했습니다. 두 문자열은 항상 같은 값으로 맞추고, JS든 CSS든 뭔가 바뀔 때마다 둘 다 같이 올리세요. 플레이어가 실제로 알아챌 만한 변경이라면 `config/patchNotes.js` 항목(아래 "패치 노트·업데이트 체커" 참고)도 같이 추가하세요 — 이걸 최소 한 번 깜빡한 적이 있어서, 나중에 생각나면 하는 게 아니라 매번 챙기는 표준 마무리 단계로 취급해야 합니다.

**NAS 배포는 git pull 방식이고 실제로 동작하는 게 확인됐습니다**(2026-07-31에 확정 — 그 전에는 NAS 사본이 수동 zip 업로드 방식 때문에 재작성 이전의 오래된 커밋에 멈춰있었다는 걸 발견해서 정리함). NAS 웹 루트는 이 저장소를 `master` 브랜치로 추적하는 진짜 git clone이고, NAS의 예약 작업이 짧은 주기로 `git pull origin master`를 자동 실행합니다. 즉 `master`에 merge(또는 push)하는 건 상징적인 행위가 아니라 실제로 거의 즉시 일어나는 배포 행위입니다 — 그렇게 대하시고, push와 실제 반영 사이에 별도의 수동 복사 단계가 있다고 가정하지 마세요. 리더보드 기능(아래 아키텍처 참고)이 들어간 뒤로는 "그냥 정적 파일"이 아닙니다 — NAS에 올릴 때 `api/` 폴더(PHP 8.2, Synology Web Station에서 사용 가능함을 확인함)도 같이 올려야 하고, `api/data/` 디렉터리는 쓰기 권한이 있어야 합니다. 안 그러면 게임 자체는 잘 뜨는데 기록 등록만 조용히 실패합니다.

## 커맨드

이 저장소에는 빌드/린트/테스트 도구가 없습니다(`package.json` 없음). 개발 루프는 다음과 같습니다.

- **로컬 실행**: 프로젝트 루트를 아무 정적 파일 서버로 서빙하고 `index.html`을 엽니다. 예: 프로젝트 루트에서 `python3 -m http.server`. `api/*.php`(리더보드)가 관련된 작업이라면 `php -S localhost:8000`을 대신 쓰세요 — 일반 정적 서버는 PHP를 실행할 수 없습니다.
- **캐시 무효화**: `index.html`의 모든 `<script>`/`<link>` 태그는 `?v=YYYYMMDD-N` 쿼리 문자열을 달고 있습니다. JS/CSS를 바꿀 때마다 이 문자열을 올려야, NAS에서 서빙되는 정적 파일이 브라우저 캐시 때문에 옛날 버전으로 보이는 일이 없습니다.
- **자동화된 테스트 프레임워크는 없습니다.** 개발 중 빠르게 헤드리스로 로직만 확인하고 싶을 때는, `jsc -m`(JavaScriptCore CLI, macOS에 `/System/Library/Frameworks/JavaScriptCore.framework/Versions/A/Helpers/jsc` 경로로 기본 내장됨)로 ES 모듈을 브라우저 없이 바로 실행해 `js/algorithms/floodFill.js`, `js/content/mechanics/*` 같은 순수 로직을 스모크 테스트할 수 있습니다 — DOM을 건드리는 걸 import한다면 먼저 `globalThis`에 `window`/`document`/`performance`/`requestAnimationFrame`을 스텁으로 채워야 합니다. `js/services/leaderboardApi.js`나 `states/gameOverState.js`를 건드리는 테스트라면 `fetch`도 스텁해야 하고, `document` 스텁도 `{ textContent: '' }` 정도로는 부족합니다 — `render/leaderboardPanel.js`가 실제로 `classList`/`addEventListener`/`focus`/`appendChild`/`createElement`를 다 호출하기 때문입니다. 이건 정식으로 커밋된 테스트 스위트가 아니라 그냥 하나의 기법이고, 테스트 스크립트는 저장소 밖 스크래치 디렉터리에 둡니다(커밋 안 함).
- **순수 로직을 넘어선 화면/DOM 확인이 필요할 때는**, 헤드리스 크롬(`google-chrome --headless=new --disable-gpu --no-sandbox`에 `--screenshot=경로.png` 또는 `--dump-dom` 추가)으로 로컬에 띄운 서버를 대상으로 콘솔 에러 없이 페이지가 뜨는지 확인하고 실제 렌더링을 눈으로 보는 게 확립된 방법입니다 — 매번 사용자에게 중간 단계를 일일이 확인해달라고 할 필요가 없습니다. 결국 게임플레이의 실제 "느낌"은 진짜 브라우저에서 직접 플레이해서 확인해야 하고, PHP 엔드포인트(`api/*.php`)는 실제 NAS에서 직접 호출해봐야 합니다 — `php -S` 밖에서는 이 저장소 안 어디서도 PHP를 실행할 방법이 없습니다.

## 아키텍처

**네이티브 ES 모듈, 번들러 없음.** `index.html`은 단 하나의 `<script type="module" src="js/main.js">`만 로드하며, 모든 의존성은 스크립트 태그 순서가 아니라 `import`/`export`로 해석됩니다. 새 파일을 추가할 때는 그래프 어딘가에 명시적인 `import`로 연결해야 합니다 — 자동 탐색은 없습니다.

**두 가지 좌표계.** 게임/충돌 로직은 전부 *격자 셀* 좌표(`config/constants.js`의 `GRID_W × GRID_H` 범위 내 정수)로만 동작합니다. `render/*`와 `core/gridMath.js`(`toPixel`/`toCell`, 또는 카메라 기준인 `toScreenX`/`toScreenY` — 아래 "카메라·뷰포트" 참고)만 픽셀로 변환합니다. 격자 좌표로 로직을 짜고, 그리는 시점에만 변환하세요.

**`World` 객체**: `game.js`는 하나의 `world` 객체(`{ snake, enemyManager, itemManager, projectileManager, eventBus, stats, camera, startTime, testMode, reset() }`)를 만들어 상태/시스템/매니저에 참조로 넘깁니다. 게임을 재시작해도 `world` 객체 자체를 새로 만들지 **않습니다** — `world.reset(options)`을 호출해서 `world`의 필드들을 제자리에서 다시 채웁니다(새 `Snake`, 새 `EnemyManager` 등). 이게 중요한 이유: 상태(state)와 시스템은 반드시 호출 시점에 `world.snake` / `world.enemyManager` 등을 통해 읽어야지, 생성 시점에 그 참조를 구조분해해서 캐싱해두면 재시작이 반영되지 않습니다. `reset`의 옵션은 `{ testMode, buildId }`입니다(둘 다 기본값은 falsy) — 타이틀 화면에서 `t` 키를 누르면 테스트-빌드-선택 화면(더 아래 "테스트 모드 빌드 선택" 참고)으로 가는데, 거기서 어떤 빌드를 고르든 기본값(`SNAKE_INITIAL_LENGTH`/`TICK_MS`) 대신 `TEST_MODE_INITIAL_LENGTH`/`TEST_MODE_TICK_MS`로 시작해서, 뱀이 길거나 속도가 빨라야 나타나는 동작(예: `sentinel`/`turret`의 길이 20 스폰 조건)을 굳이 거기까지 플레이해서 도달하지 않고도 바로 시험해볼 수 있습니다. `entities/snake.js`의 `Snake` 생성자가 초기 길이를 매개변수로 받는(`initialLength = SNAKE_INITIAL_LENGTH`) 것도, `reset`이 두 상수 중 하나를 그대로 넘겨줄 수 있게 하기 위해서입니다. `world.stats`는 원래 상수를 직접 읽던 값들의 "이번 판 현재값"을 담는, 계속 커지고 있는 값 모음입니다(`tickMs`, `attackDamage`, `scaleWaveDamage`, `enemySpawnMinMs`/`MaxMs`, `enemyKillStacks`, `killsByType`, 그리고 더 아래 각자 항목에서 다루는 cintamani/점수/무기 잠금 관련 필드들) — 그 원래 상수들은 이제 재시작할 때 되돌아가는 기본값일 뿐이고, 실시간 값이 필요한 곳(게임 루프의 틱 간격, `fire()`의 발사체 공격력, `fireScaleWave()`의 범위 공격력, HUD, `enemyManager`의 스폰 타이밍)은 전부 `world.stats.*`를 읽어야 합니다. `speedUp`/`speedDown`/`attackUp` 같은 아이템이, 그리고 2026-08-03부터는 테스트 모드의 `buildId` 오버라이드도 이 값을 바꾸기 때문입니다. `attackUp`(주황 먹이)은 한 번 먹을 때 `attackDamage`(`ATTACK_UP_DELTA`, +1)와 `scaleWaveDamage`(`ATTACK_UP_SCALE_WAVE_DELTA`, +3) 둘 다 올립니다 — 독침과 비늘파동이 같은 비율로 세지지 않으므로 증가량 상수를 하나로 공유하지 않고 따로 둡니다. `core/loop.js`가 고정된 `tickMs` 대신 `getTickMs()` 콜백을 받는 것도 같은 이유입니다 — 루프가 이미 시작된 뒤에도 이동 간격이 바뀔 수 있습니다. `enemyKillStacks`는 `{ [적타입id]: 처치수 }` 형태의 단순한 맵으로, `world.reset()`마다 새로 초기화되며, 어떤 적 타입의 `onDefeated` 훅에서든 여기에 누적시킬 수 있습니다(`basic.js` 참고) — 특정 타입에 종속되지 않고 범용으로 만든 이유는, 어떤 적이 이 처치-스택 보상을 갖는지가 이미 한 번 바뀌었기 때문입니다(원래 `chaser.js`에 있다가 `basic.js`로 옮겨감), 그리고 앞으로도 또 바뀔 가능성이 큽니다.

**프레임 단위 업데이트와 분리된 고정 틱 게임 루프** (`core/loop.js`): `createGameLoop`은 `requestAnimationFrame` 루프를 돌리면서, 매 프레임 `onFrame(dt)`를 호출하고(연속적인 것들: 발사체 이동, 먹이 스폰 타이머), 누적 시간이 `TICK_MS`를 넘을 때 프레임당 최대 한 번 `onTick()`을 호출합니다(이산적인 격자 이동: `snake.step`, 충돌, 적 스폰 타이머). 이건 밀린 틱을 몰아 처리하는 `while` 루프가 아니라 단순 `if` 하나입니다 — 원래의 타이밍 모델과 정확히 동일하니, 실제로 원하는 게 맞는지 확인 없이 "따라잡기(catch-up)" 루프로 "고치지" 마세요.

**프레임 사이 간격이 크게 벌어지면, 그 시간을 그대로 반영하지 않고 잘라냅니다** (`core/loop.js`의 `MAX_FRAME_GAP_MS = 250`): 탭이 백그라운드로 가거나 OS가 잠들면, 다음 `requestAnimationFrame` 콜백의 `timestamp`가 이전 프레임보다 몇 초(심하면 몇 분)씩 앞서 있을 수 있습니다. 이걸 그대로 두면 두 가지가 문제입니다 - `onFrame(dt)`에 비정상적으로 큰 `dt`가 들어가는 것도 있지만, 더 심각한 건 복귀 후 여러 프레임에 걸쳐 틱 판정(`timestamp - lastTick >= tickMs`)이 계속 참이 된다는 겁니다. 틱 분기가 (위에서 설명한 대로) 단순 `if` 하나라서, 그 여러 프레임 각각이 "따라잡기" 틱을 하나씩 추가로 쏴버립니다 - 즉 뱀이 `tickMs`(예: 120ms)마다 한 번이 아니라 *렌더링되는 프레임마다*(예: 16ms마다) 한 번씩, 밀린 게 다 풀릴 때까지 계속 움직이게 되고, 이게 눈에는 벽이나 자기 몸으로 순간이동해서 부딪히는 것처럼 보입니다. 실제로 제보받은 버그였습니다: 게임오버 화면에서 가만히 놔두면(그 화면의 `onTick()`은 아무것도 안 하니 밀린 틱이 백그라운드 상태에서 조용히 계속 쌓이는데) 그게 다 안 풀린 채로 재시작을 누르면, 남은 따라잡기 폭주가 막 시작한 `playing` 상태로 그대로 넘어가서 거의 즉시 죽어버렸습니다. 고친 방법: 실제 프레임 간격이 `MAX_FRAME_GAP_MS`를 넘으면, `onFrame`에 넘기는 `dt`와 `lastTick` 둘 다 현재 프레임 기준 최대 그만큼만 뒤처진 것으로 잘라냅니다 - 밀린 걸 나중에 처리하는 게 아니라 애초에 버리는 거라, "따라잡기 루프"와는 정반대입니다.

**게임 상태 머신** (`core/stateMachine.js` + `states/*.js`): 현재는 `title`, `testBuildSelect`, `playing`, `gameOver`, `leaderboardView` 다섯 가지이며, 시작 상태는 `playing`이 아니라 `title`입니다. 각 상태는 `{ enter(payload), exit(), onFrame(dt), onTick(), render() }` 형태입니다. `game.js`의 루프는 항상 `stateMachine.current`에 위임합니다. 각 상태는 공용 `input/actions.js` 싱글턴을 통해 `enter()` 시점에 자기 키 액션을 바인딩하고 `exit()` 시점에 해제합니다(예: `playingState`는 Space→발사를 바인딩). `gameOverState`는 `enter(payload)`가 `async`이고 곧바로 Enter→타이틀 복귀를 바인딩하지 않는 유일한 상태입니다 — 이유는 아래 리더보드 문단 참고. `titleState`는 특정 키 세 개를 바인딩합니다: `Enter`(`onStart({testMode:false})`로 곧바로 일반 판 시작), `t`(곧바로 판을 시작하는 대신 `testBuildSelect` 상태로 전환 — 더 아래 "테스트 모드 빌드 선택" 참고), `l`(리더보드 열람). 실제로 `world.reset(options)`을 호출하고 `playing`으로 전환하는 건 `onStart`(사실은 `game.js`의 `startGame`)인데, `titleState`의 `Enter`에서 직접 호출되든 빌드를 고른 뒤 간접적으로 호출되든 마찬가지입니다. (예전엔 "아무 키나 누르면" 시작되는 범용 `Actions.bindAny(onStart)` 폴백을 썼는데, 타이틀 화면이 "어떤 키가 눌렸는지" 구분해야 하게 되면서 제거했습니다 — 그 뒤로 `bindAny`를 쓰는 곳이 코드베이스에 하나도 안 남아서, 쓰이지 않는 채로 남겨두지 않고 `input/actions.js`에서 아예 지웠습니다.) 그래서 어디에도 `if (gameOver)` 같은 명시적 체크 없이도 Enter 재시작이 `gameOver` 상태일 때만 동작하는 것이고, 흐름은 항상 타이틀 → (선택적으로 testBuildSelect →) 플레이 → 게임오버 → 타이틀이며 게임오버에서 플레이로 바로 가는 경로는 없습니다. `input/actions.js`의 `normalizeKey`는 매칭 전에 알파벳 한 글자 키를 소문자로 바꾸므로, `bind('t', ...)`는 Shift나 Caps Lock 상태와 무관하게 걸립니다 — 나중에 다른 알파벳 키를 바인딩할 때 알아두면 좋습니다. **`e.key`로 폴백하기 전에 먼저 `e.code`를 확인합니다** - 한글(또는 그 외 비영문) 입력기가 켜져 있으면 `e.key`는 그 레이아웃에서 *조합된* 문자를 담는데(예: 2벌식에서 물리적 P 키는 `'ㅔ'`로 나옴), 물리 키 자체를 담는 게 아닙니다 - 그래서 `bind('p', ...)` 등이 OS 입력 언어가 영문이 아닌 순간 조용히 안 먹게 되는 실제 신고된 문제(일시정지/리더보드/테스트모드 키가 한글 입력 상태에서 반응 안 함)가 있었습니다. `e.code`(예: `'KeyP'`)는 레이아웃/입력기 상태와 무관하게 항상 물리적 키를 가리키므로, `normalizeKey`는 먼저 `e.code`를 `/^Key([A-Z])$/`로 매칭해보고, 그 패턴에 안 맞을 때만(알파벳이 아닌 `Escape`/`Enter` 등, 또는 테스트 스텁처럼 `e.code`를 아예 안 채워주는 환경) 예전의 `e.key` 기반 경로로 넘어갑니다.

**이동 방향 입력은 슬롯 하나가 아니라 짧은 큐입니다** (`input/input.js`): `Input.consume()`은 게임 틱마다 한 번씩 호출되는데(`playingState.js`의 `onTick()`), 최고 속도에서는 틱 하나가 `MIN_TICK_MS`(40ms)만큼 짧아질 수 있습니다. 원래 버전은 `pending`이라는 방향 하나만 저장해뒀다가 다음 `keydown`이 오면 그냥 덮어썼는데 — 실제로 제보된 버그였습니다: 한 틱(40~120ms, 특히 반사신경이 빠르거나 익숙한 플레이어라면 충분히 도달 가능한 범위) 안에 방향키를 두 번 누르면 *두 번째* 입력만 살아남고 첫 번째는 조용히 사라졌습니다("입력이 씹힘"). 이를 고치려고 최대 `MAX_QUEUE`(2)개까지 방향을 FIFO 큐에 버퍼링하도록 바꿨습니다 - `consume()`이 항상 같은 `applied` 값을 돌려주는 대신 매번 큐에서 하나씩 꺼내므로, 빠르게 두 번 누른 입력이 이제 하나로 뭉개지지 않고 다음 두 틱에 걸쳐 각각 따로 적용됩니다. 역주행 방지 검사(`isReverse`, 180도 자기 반전을 막음)에는 슬롯 하나짜리 버전에 숨어있던 더 미묘한 두 번째 버그도 있었습니다: 새 키 입력을 오직 `applied`(*직전 틱*의 방향)하고만 비교했지, 아직 적용 전인 `pending`에 들어있는 방향하고는 비교하지 않았습니다 - 그래서 오른쪽으로 이동 중에 위→아래를 빠르게 연달아 누르면, 두 번째 입력(아래)이 곧 적용될 "위"가 아니라 이미 지나간 "오른쪽" 기준으로 검증되면서 원래는 막혔어야 할 역주행이 통과해버릴 수 있었습니다. 큐 버전은 큐가 비어있지 않을 때 새 키 입력을 `queue[queue.length - 1]`(가장 최근에 *예약됐지만* 아직 적용 안 된 방향) 기준으로 검사하고, 큐가 비어있을 때만 `applied`로 대체하는 방식으로 이걸 고쳤습니다 - 즉 이 새 입력이 실제로 적용될 시점에 뱀의 방향이 뭐가 될지를 기준으로 항상 검증합니다.

**`Input`은 페이지가 떠 있는 내내 사는 싱글턴이라, 명시적으로 `reset()`을 호출해줘야 합니다 - `world.reset()`은 이걸 안 건드립니다.** 이번 판의 다른 모든 상태는 `world`에 있어서 `world.reset()`이 매번 새로 갈아끼우는데(위 `World` 객체 항목 참고), `input/input.js`의 `queue`/`applied`는 모듈 최상단 변수라 구조상 재시작해도 그대로 남습니다 - 새 게임을 시작한다고 이 모듈을 다시 import하는 일은 없으니까요. 이건 위 큐 버그와 밀접하게 연결된 두 번째 실제 제보 버그였습니다: 게임오버 화면에서(혹은 죽기 직전에) 무심코 누른 방향키가 큐/적용 방향에 그대로 남아있다가, 플레이어가 새로 아무것도 안 눌렀는데도 *다음* 판이 시작되자마자 발동됐습니다 - 잘해야 의도치 않은 이른 회전이고, 최악의 경우(그 남은 방향이 뱀의 시작 몸통 방향으로 그대로 걸어들어가는 방향이면) 첫 틱에 즉시 자기충돌로 죽어서 "게임이 시작하자마자 벽/몸통으로 돌진한다"처럼 보였습니다. `playingState.js`의 `enter()`가 이제 기존의 `pendingDirection = null`과 함께 `Input.reset()`(큐를 비우고 `applied`를 기본 오른쪽 방향으로 되돌림)도 호출합니다 - `pendingDirection = null`만으로는 절대 충분하지 않았다는 점도 짚어둘 만한데, `onTick()`이 첫 틱에 무조건 `Input.consume()`이 돌려주는 값으로 그걸 덮어써버리기 때문입니다.

**일시정지는 상태머신의 상태가 아니라, 일부러 `playingState` 안의 플래그로만 처리합니다.** `P`와 `Escape` 둘 다 로컬 `paused` 불리언을 토글하는 `togglePause()`를 호출합니다(기존 `Space`→`fire` 바인딩과 나란히 `enter()`/`exit()`에서 bind/unbind). 진짜 `'paused'` 상태를 만드는 것도 고려했다가 기각했습니다 - `playing`에서 벗어났다가 재개 시 다시 들어오면 `playingState.exit()`/`enter()`가 다시 돌 텐데, `enter()`는 일부러 여러 예민한 진행 중 상태(`Input.reset()`, `pendingDirection = null`, `passThroughGrace = false` - 위 `Input` 싱글턴, 통과 봐주기 항목 참고)를 리셋합니다. 진짜 일시정지라면 이것들을 절대 건드리면 안 됩니다 - 예를 들어 일시정지 도중 `passThroughGrace`를 잃으면, 방금 닫힌 고리 안에서 재개한 직후 정상적인 자기충돌이 잘못 통과될 수 있습니다. 플래그로 유지하면 `onFrame`/`onTick`이 `paused`일 때 그냥 바로 반환하기만 하면 되고(타이머/이동/충돌판정 등 모든 게 `passThroughGrace`를 포함해 손대지 않은 채로 멈춤), `render()`는 매 프레임 평소대로 다 그린 뒤 그 위에 `render/overlays.js`의 `renderPauseOverlay(ctx)`(어둡게 + "PAUSE" 텍스트, `renderGameOverOverlay`와 같은 시각 언어)와 `statusPanel.renderDimOverlay()`를 겹쳐 그립니다 - 그래서 멈춘 게임/상태창이 안 보이는 게 아니라 어둡게 계속 보입니다. `fire()` 자신도 `paused`를 확인해서 아무 것도 안 하는데, `Space`는 일시정지 내내 바인딩된 채로 두기 때문입니다.
- **일시정지해 있던 실제 시간은 생존시간/점수 계산에서 제외되는데, 이걸 쓰는 곳마다 일일이 고치는 대신 `world.startTime` 하나만 보정합니다.** `survivalMs`와 HUD의 실시간 `survivalSeconds` 둘 다 각자 호출 지점에서 `performance.now() - world.startTime`으로 계산됩니다 - 이 각각에 일시정지 인식 로직을 심는 대신, `togglePause()`가 일시정지할 때 `pauseStartedAt`을 기록해두고, 재개할 때 `world.startTime += performance.now() - pauseStartedAt`을 합니다. 딱 한 곳은 직접 손봐야 했는데: `render()`의 실시간 `survivalSeconds`는 "지금"으로 `performance.now()`를 쓰는데, 이러면 일시정지하는 내내 계속 올라가버립니다 - `paused`가 true인 동안은 "지금" 기준을 `pauseStartedAt`으로 바꿔서 고쳤습니다.

**원거리 무기 두 종류, 둘 다 판 안의 조건으로 잠겨있고, 둘 다 스페이스바를 같이 씁니다.** `world.stats.venomUnlocked`(독침)는 `onTick`에서 성장이 해소된 뒤 `snake.segments.length >= VENOM_UNLOCK_LENGTH`(8)가 이번 판 처음 관측되는 순간 true가 됩니다. `world.stats.scaleWaveUnlocked`(비늘파동)는 `venomUnlocked`가 이미 true이고 동시에 `killsByType[chaserEnemy.id] >= SCALE_WAVE_UNLOCK_CHASER_KILLS`(5)일 때 true가 됩니다 - 이 선후관계는 의도적입니다: 비늘파동의 상호작용 방식 자체("완전 충전 전에 손을 떼면 독침으로 대체")가 이미 독침이 동작한다는 걸 전제로 합니다. 두 확인은 `if / else if` 체인으로 묶여 있어 한 틱에 튜토리얼 팝업이 최대 한 개만 뜹니다. 두 플래그 모두 한 번 true가 되면 이후 길이/킬 수가 바뀌어도 이번 판 내내 true로 유지됩니다.
- **무기를 처음 잠금 해제하면 게임이 멈추고 작은 팝업이 뜨는데, 반드시 Enter를 눌러야만 닫힙니다.** `showTutorial(...)`이 일시정지시킨 뒤 `Actions.bind('Enter', dismissTutorial)`을 호출합니다. 예전엔 아무 키나 누르면 닫혔는데, 방향키를 무심코 눌렀다가 팝업이 닫혀버리는 문제가 있어서 Enter 전용으로 바꿨습니다. `render/overlays.js`의 `renderTutorialPopup`은 아이콘(무기 상징색 원으로 폴백) + 굵은 제목 + 작은 설명 세 단으로 그립니다. **테스트 모드는 튜토리얼은 건너뛰지만 잠금 해제 자체는 그대로 됩니다.** `TEST_MODE_INITIAL_LENGTH`가 이미 `VENOM_UNLOCK_LENGTH`를 넘어있어서, 그냥 두면 테스트 모드 판은 첫 틱에 튜토리얼 팝업을 맞게 되는데 이건 테스트 모드의 취지를 무너뜨립니다. **테스트 모드는 `killsByType[chaserEnemy.id]`도 미리 채워둡니다**(`game.js`의 `world.reset()`) - 그래야 비늘파동도 독침 켜진 바로 다음 틱에 같이 켜집니다.
- **스페이스바는 독침이 걸리는 순간부터 `Actions.bind`가 아니라 `playingState.js`가 직접 갖는 raw `keydown`/`keyup` 리스너로 처리됩니다.** 비늘파동 충전 메커니즘이 스페이스바를 *얼마나 오래* 눌렀는지 알아야 해서 `keyup`이 필요한데, `Actions`는 그 기능이 없습니다. 잠금 상태에 따라: 잠금 전엔 아무 것도 안 함; 독침만 풀렸으면 keydown에 즉시 단발 발사(`e.repeat` 가드로 연사 방지); 비늘파동까지 풀렸으면 keydown이 충전 타이머를 시작하고 keyup이 누른 시간을 재서 `SCALE_WAVE_CHARGE_MS`(1000ms) 이상이면 비늘파동, 아니면 독침으로 대체 발사합니다.
- **`renderChargeGauge`**가 머리 위에 충전 게이지를 그립니다 - 매 프레임 `chargeStartAt` 기준으로 다시 계산해서 항상 실제 경과 시간과 일치합니다.
- **`fireScaleWave()`는 뱀 몸 전체의 각 칸을 중심으로 "+"자 모양 범위를 합쳐서 판정 범위로 씁니다** - 머리 주변 하나의 덩어리가 아니라. 데미지는 `enemyManager.applyProjectileHit`을 거치므로, 투사체에 면역인 적(2/4번)은 비늘파동에도 면역입니다.
- **`grantKillReward`**는 처치 시 항상 같이 일어나는 네 가지(파티클, `killScore` 증가, 점수 팝업, `killsByType` 증가)를 묶은 `playingState.js`의 헬퍼입니다 - `managers/projectileManager.js`, `content/cintamani/shared.js`의 `killEnemyForCintamani`(더 아래 참고)도 각자 자기만의 거의 동일한 사본을 갖고 있습니다 - 다른 모듈 경계를 넘어 공유하기보다 각자 갖는 걸 택한 전례가 이제 세 곳입니다.
- **투사체 중심 정렬 버그 수정**: `projectileLayer`가 셀의 실제 중앙이 아니라 투사체 자신의(더 작은) 지름 기준으로 원을 그려서, 모든 독침이 셀 좌상단 쪽으로 살짝 치우쳐 보였던 문제를 고쳤습니다.

**이벤트 버스** (`core/eventBus.js`): 결합을 느슨하게 하는 반응에만 씁니다 - 같은 틱 안에서 순서가 중요한 결정에는 안 씁니다. `playingState`가 진짜(봐주기 대상 아닌) 충돌로 판이 끝나면 `'playerDied'`를 발행하고, `game.js`가 유일한 구독자로서 `gameOver`로 전환합니다. 충돌 판정 자체(벽 → 메커닉 → 자기충돌 → 적 → 먹이)는 리스너 등록 순서에 따라 재배열되면 안 되므로 `systems/collision.js`/`states/playingState.js`의 직접 동기 함수 호출로 남아있습니다.

**`content/`는 확장 지점입니다 - 적, 아이템, 포획형 메커닉, cintamani 스킬 전부 레지스트리 기반입니다.** 각 분류(`content/enemies/`, `content/items/`, `content/mechanics/`, `content/cintamani/`)는 같은 패턴을 따릅니다: def 파일은 레지스트리에 의존하지 않는 순수 설정 객체를 export하고, 그 분류의 `index.js`가 각 def 파일을 import해서 `registry.register(def.id, def)`를 호출합니다. def 파일이 자기 분류의 레지스트리를 직접 import해서 스스로 등록하면 **안 됩니다** - def 파일과 `index.js` 사이 순환참조/TDZ 오류를 피하려면 상수를 받아 def 객체를 내놓는 단방향 import만 유지해야 합니다. 새 적 타입, 아이템, 메커닉, cintamani 색상을 추가하는 건: 새 def 파일 하나 + 해당 분류 `index.js`에 import+register 한 줄 - `managers/*`, `systems/collision.js`, `render/*`는 절대 안 바꿉니다.

- **적 타입** (`content/enemies/`): 설정 필드는 `color, hp, canBeDamagedByProjectile, displayText(enemy), collidesWithHead`, 그리고 아래의 점점 늘어나는 선택적 훅들입니다. `managers/enemyManager.js`는 절대 타입 id를 특수 취급하지 않습니다 - 적 인스턴스는 스폰 시점에 한 번 정해진 `typeDef` 참조를 갖습니다. `checkHeadCollision`은 적의 `ENEMY_SCALE × ENEMY_SCALE` 전체 그려지는 영역을 히트박스로 취급합니다.
  - `spawnEligible(world)`가 스폰 가능 여부를 결정합니다. `sentinel.js`/`turret.js`는 길이 조건(`LONG_SNAKE_UNLOCK_LENGTH`)을 공유하고, `hunter.js`는 `world.stats.tickMs` 조건을 씁니다. `_trySpawn(world)`은 `spawnEligible` 필터링 전에 `ENEMY_MAX_COUNT` 하드 캡부터 확인합니다. `_trySpawn`은 칸보다 타입을 먼저 고릅니다 - captureZone 타입은 자기 포획존 정사각형 전체가 격자 안에 있어야 해서, 어떤 타입인지 미리 알아야 여백을 계산할 수 있습니다. captureZone 적이 가장자리에 딱 붙어 스폰되면 영구히 포획 불가능해지는 문제, 적이 뱀 머리에 너무 가까이 스폰되는 문제, captureZone 적 둘이 너무 붙어 스폰돼 한쪽만 감쌀 수 없게 되는 문제 - 이 세 가지 모두 후보 칸 스캔에서 필터로 막아둡니다.
  - `captureZone: { scale }`은 포위 메커닉(아래)에 참여시킵니다.
  - `move(enemy, world)` + `moveIntervalMs(enemy, world)`는 자율 이동을 부여합니다 - `chaser.js`/`hunter.js`는 뱀의 실시간 속도(`world.stats.tickMs`)와 무관한 고정 간격으로 움직입니다(속도 먹이가 이 적들의 이동 속도를 바꾸면 안 된다는 명시적 수정). `chaser.js`의 배회는 완전 무작위가 아니라 한동안 한 방향을 유지하는 편향된 랜덤워크이고, 가끔 그 방향과 무관하게 한 걸음만 무작위로 튀는 지터가 섞입니다.
  - `onDefeated(world, enemy)`는 투사체로 처치됐을 때만(포획 아님) 한 번 발동합니다 - `basic.js`는 처치 스택 보상, `chaser.js`는 처치할 때마다 즉시 그 자리에 노랑 먹이 스폰에 씁니다.
  - `onCaptured(world, enemy)`는 포위 메커닉으로 제거됐을 때의 대응 훅입니다 - `sentinel.js`는 확률적 주황 먹이, `turret.js`는 3갈래 가중치 드롭 테이블에 씁니다. 어떤 보상이 어떤 적에 붙어 있는지는 이미 한 번 재배정된 적 있어서, 상수 이름은 적이 아니라 보상 자체를 기준으로 짓습니다.
  - `ability(enemy, world, dt)` + `abilityCooldownMs(enemy, world)`는 이동 외의 시간제 부수효과(예: 발사)를 부여합니다 - 쿨다운은 발동 자체만 게이팅하고, 조건 판정은 쿨다운이 찬 뒤로 매 프레임 계속 재시도합니다.
- **아이템** (`content/items/`): 설정 필드는 `color`와 `onPickup(world, item)`, 선택적으로 `spawnEligible(world)`(적과 같은 게이팅 패턴), `overlayText`(아이템 칸 위에 그리는 작은 흰 글씨, 예: `tripleGrowth.js`의 `'x3'`)입니다. 지금은 초록 먹이만 `spawnEligible: () => false`가 아닌 유일한 것이고, 나머지(speedUp/speedDown/tripleGrowth/attackUp)는 전부 보상 전용이라 앰비언트 타이머로는 절대 안 나오고 특정 적의 `onDefeated`/`onCaptured`에서만 나옵니다.

  `managers/itemManager.js`는 `spawnItem`(앰비언트, `FOOD_MAX_COUNT` 상한 적용), `spawnSpecific`(무작위 빈 칸, 상한 무시), `spawnAt`(정확한 좌표, 상한 무시) 세 가지 추가 방식을 씁니다 - 상한 구분은 의도적입니다: `FOOD_MAX_COUNT`는 수동적인 앰비언트 스포너에만 적용되고, 전투 보상은 필드가 이미 먹이로 붐빈다고 조용히 안 나오면 안 됩니다.
- **메커닉** (`content/mechanics/`): 각각 `{ id, suppressesSelfCollision, tick(world, outlineCells) }` 형태입니다. `content/mechanics/encirclement.js`가 현재(그리고 아직은 유일한) 메커닉입니다: `algorithms/floodFill.js`(격자 바깥에서부터의 BFS)로 뱀 몸 바깥에서 도달 가능한 칸을 계산하고, 적의 칸이 위상적으로 봉쇄됐는지 판정합니다. 포획은 봉쇄 안 됨→봉쇄됨으로 전환되는 그 틱에만(적별 `WeakMap`으로 추적) 발동하고, 그 순간 뱀 머리가 포획존 안에 있고 봉쇄한 고리가 그 포획존 안에 완전히 들어와야 합니다.

**충돌 순서는 손으로 짠, 협상 불가능한 순서입니다** (`states/playingState.js.onTick()`, `systems/collision.js`가 뒷받침): 벽 → 모든 메커닉 실행 → 자기충돌(봐주기, 아래 참고) → 적과의 정면충돌(항상 치명적) → 먹이 섭취 → 성장 → 적 스폰 타이머 갱신. 이 순서를 건드리면 메커닉과 자기충돌 체크를 분리하는 것 같은 실수가 "포획 후 통과 봐주기"를 깨뜨립니다.

**발사체는 `owner`를 갖고(`entities/projectile.js`, 기본값 `'player'`), `managers/projectileManager.js.update(dt, world)`는 이걸 기준으로 충돌 정책 전체를 분기합니다** - 플레이어 발사체는 뱀을 무시하고 적에게 데미지를 주고, `owner: 'enemy'` 발사체는 적을 무시하고 대신 머리에 닿으면 항상 게임을 끝내거나 몸통에 닿으면 발사체 자신의 `onBodyHit(world)` 콜백을 부릅니다. **`owner: 'enemy'` 발사체가 뱀 머리/몸통 어느 것에도 안 맞으면 다른 모든 적과도 판정합니다** - 두 적이 근처에 같이 있는 게 흔해진 뒤로, 터렛 탄환이 다른 적을 그냥 통과하는 게 이상해 보인다는 신고로 추가됐습니다. 발사체의 머리 접촉 판정은 `onTick()` 시퀀스 바깥, `onFrame()`에서 즉시 처리됩니다 - 발사체는 프레임마다 연속적으로 움직이므로, 다음 틱까지 기다리면 치명타를 맞고도 한 틱만큼 그대로 통과하는 것처럼 보일 수 있어서입니다.

**`checkEnemyHeadCollision`은 `onTick()`과 `onFrame()` 둘 다에서 확인합니다 - 중복이 아니라 서로 다른 두 경우를 보완합니다.** `onTick()` 쪽은 머리가 `snake.step()`으로 적 칸에 들어가는 걸 잡습니다. 하지만 이동형 적(chaser/hunter)은 `onFrame`의 `updateMovement`에서 자기 시계로 움직이는데, 이게 뱀의 틱보다 빠를 수 있어서 `onTick()` 확인만으로는 두 틱 경계 사이에 적이 머리 칸을 밟았다가 빠져나가버릴 수 있습니다 - 실제로 "히트박스가 그려진 것보다 작게 느껴진다"는 신고가 있었던 버그입니다. `onFrame()`이 `updateMovement` 직후 이 확인을 다시 해서 즉시 `die()`를 부르는 걸로 고쳤습니다.

**통과 봐주기는 포획 순간 그 틱 하나가 아니라 여러 틱에 걸쳐 유지됩니다** (`passThroughGrace`): captureZone 적을 감싼 고리는 대개 빈 칸으로 이동하며 닫혀서(그 틱엔 자기충돌 없음, 적만 깔끔히 제거됨) 실제 몸통 교차는 한 틱 이상 나중에, 뱀이 자기가 방금 만든 고리 안에 갇혀서 빠져나오려고 자기 몸을 통과해야 할 때 일어납니다. `passThroughGrace`는 메커닉이 뭔가를 포획한 틱마다 true로 설정되고, 이후 자기충돌인 틱 내내 true로 유지되며(고리를 빠져나오는 데 몇 틱이 걸리든), 머리가 빈 칸에 착지한 틱에만(뱀이 정말로 빠져나왔다는 증거) 풀립니다.

**성장은 슬롯 하나가 아니라 큐입니다** (`entities/snake.js`, `growQueue`): 먹이를 먹으면 대기 중인 성장 위치를 예약하고, `checkGrowth()`는 뱀의 몸이 그 칸을 완전히 비운 뒤에야 각 대기 항목을 독립적으로 해소합니다. 먹이가 먹히자마자 바로 리스폰돼서, 첫 번째 성장이 아직 해소되기 전에 두 번째 먹이를 먹는 게 일상적이라 큐여야 합니다. 줄어드는 건(`snake.shrink(n)`, `speedDown` 아이템용) 즉시 처리되고 큐를 안 씁니다.

**렌더링은 순서가 있는 레이어 목록입니다** (`render/layers.js`가 export하는 `layers` 배열, `render/renderer.js`가 순서대로 순회): `backgroundLayer` → `fieldBorderLayer` → `captureZoneLayer` → `blueRainLayer` → `itemLayer` → `scaleWaveLayer` → `redBeamLayer` → `snakeLayer` → `projectileLayer` → `enemyLayer` → `particleLayer` → `scorePopupLayer` → `versionLayer`. `render/hud.js`는 DOM 스탯 바(캔버스 그리기와 별개, 상태창 자체 점수 표시가 생기면서 상당 부분 대체됨 - 아래 실시간 점수 참고)를 담당하고, `render/overlays.js`는 상태별 전체화면 오버레이(타이틀, 리더보드 열람 배경, 일시정지, 튜토리얼 팝업, 게임오버, 충전 게이지)를 그리고, `render/statusPanel.js`는 완전히 별도의 세 번째 캔버스입니다(아래 "상태창" 참고).

**필드는 화면보다 크고, 카메라가 그 위를 스크롤하는 뷰포트를 비춥니다.** `GRID_W × GRID_H`(144×108, 4:3 필드 - 4:3 이전의 와이드 레이아웃과 비슷한 플레이 가능 면적을 유지하면서 캔버스 자체의 4:3 모양과 맞춘 선택)가 필드 전체이고, 사방이 `FIELD_WALL_THICKNESS`(10)만큼 두꺼운 이동 불가 회색 벽으로 둘러싸여 있습니다(`PLAYABLE_MIN/MAX_X/Y`가 실제 이동 가능한 안쪽을 표시; `fieldBorderLayer`가 벽을 안쪽과 다른 색으로 그림). 화면에는 그 필드 중 `VIEWPORT_COLS × VIEWPORT_ROWS`(68×51, 역시 4:3)만큼의 창만 한 번에 그려지고, 칸 하나당 `CAMERA_ZOOM`(24/17) 픽셀 배율입니다 - `VIEWPORT_COLS * CELL_SIZE * CAMERA_ZOOM`과 세로 쪽 계산이 부동소수점 오차 없이 정확히 768×576 캔버스가 되도록 일부러 고른 값입니다.
- `core/camera.js`의 `world.camera`(`{x, y}`, 격자 단위, 소수 가능 - 부드러운 이동을 위해 정수로 반올림 안 함)가 뷰포트 좌상단을 추적합니다. `updateCamera(camera, headX, headY, dtSeconds)`(매 프레임 `playingState.js`의 `onFrame`에서 호출)는 뱀 머리가 화면 중앙 기준 데드존(`CAMERA_DEADZONE_RATIO`, 0.4) 밖으로 나가야만 움직이기 시작하고, 그다음엔 순간이동이 아니라 지수 감쇠(`CAMERA_FOLLOW_SPEED`)로 목표 지점까지 서서히 다가갑니다 - 이 부드러운 보간은 이 게임의 나머지 이산적(보간 없는) 이동 원칙에 대한 의도적인 예외입니다(뱀 자체가 왜 보간 안 되는지는 아래 "뱀 스프라이트 렌더링" 참고). 필드 경계 밖을 보여주지 않도록 clamp됩니다.
- `core/gridMath.js`의 `toScreenX`/`toScreenY`/`screenCellSize`만이 카메라 기준 픽셀 변환이 일어나는 곳입니다 - 카메라 기준으로 그리는 모든 `render/*` 호출은 직접 픽셀을 계산하지 않고 반드시 이걸 거쳐야 합니다. `toPixel`/`toCell`은 카메라와 무관한 경우(현재는 `render/minimap.js`뿐, 카메라 위치와 무관하게 항상 필드 전체를 보여줌)에 씁니다.
- **`core/viewportConfig.js`는 `cameraZoom`/`viewportCols`/`viewportRows`를 런타임에 바꿀 수 있는 값으로 갖고 있고, 일부러 `world.stats`와 분리돼 있습니다.** `core/gridMath.js`, `core/camera.js`, `render/minimap.js`, `render/layers.js`의 `versionLayer`(글자 크기 계산)가 전부 `config/constants.js` 값을 직접 import하는 대신 `getViewportConfig()`로 실시간으로 읽습니다. 이렇게 해둔 이유는, 테스트 모드 "빌드"(더 아래 "테스트 모드 빌드 선택" 참고)가 이 호출부들이 테스트 빌드의 존재 자체를 전혀 몰라도 다른 시야를 미리 보여줄 수 있게 하기 위해서입니다 - `setViewportConfig(overrides)`가 현재 설정 위에 부분 오버라이드를 얹고, `resetViewportConfig()`가 `config/constants.js` 기본값으로 되돌립니다. `world.stats`에 합치지 않고 별도 모듈로 뺀 이유: 이 값들은 아이템 픽업으로 판 도중 계속 바뀌는 `tickMs`/`attackDamage`류와 달리 빌드 선택 시점에 한 번 정해지는 "세션 설정값"에 더 가깝고, 위 카메라/좌표 함수들은 프레임마다 아주 자주 호출되므로 거의 안 바뀌는 숫자 세 개를 읽으려고 매번 `world`를 넘기는 게 그만한 가치가 없다고 판단했습니다.
- **어떤 오버라이드든 실제 캔버스 픽셀 크기(현재 768×576)는 반드시 유지해야 합니다.** `main.js`는 페이지 로딩 시 딱 한 번, 그 순간의 `getViewportPixelSize()` 값(아직 빌드를 안 골랐으니 항상 `config/constants.js` 기본값)으로 `#game-canvas`/`#status-canvas` 크기를 정하고, 이후로는 절대 다시 계산하지 않습니다. 그래서 `viewportCols`/`viewportRows`/`cameraZoom`을 바꾸는 오버라이드가 `viewportCols * CELL_SIZE * cameraZoom`(세로도 마찬가지)을 그 고정 픽셀 크기와 같게 유지하지 않으면, 캔버스 크기가 안 바뀌는 게 아니라 게임 세계가 기존 캔버스에 잘못 매핑됩니다. "시야 확대" 실험은 캔버스 자체를 바꾸는 게 아니라, `cameraZoom`을 낮추고 그만큼 `viewportCols`/`viewportRows`를 올려서 만듭니다.
- **`render/minimap.js`**는 카메라의 현재 뷰포트가 아니라 필드 *전체*를 고정된 `MINIMAP_WIDTH_PX × MINIMAP_HEIGHT_PX` 박스에 축소해서 그립니다 - `toScreenX`/`toScreenY`가 아니라 자체적인 단순 비례식을 씁니다(미니맵의 존재 이유 자체가 스크롤되지 않는 필드 전체 안에서 지금 카메라가 어디를 보는지 보여주는 것이므로). 벽 띠, 적/아이템/뱀 점을 그린 뒤 마지막으로 지금 카메라가 보여주는 범위를 사각형으로 겹쳐 그립니다(이것도 `getViewportConfig()`를 실시간으로 읽어서 테스트 빌드의 시야 오버라이드 아래서도 정확합니다).

**뱀은 가능하면 손그림 스프라이트시트로 그려지고, 없으면 색깔 사각형으로 대체됩니다** - 이 코드베이스의 다른 모든 아트 애셋과 같은 점진적 대체 관례입니다. `render/snakeSprites.js`가 `assets/snake/snake_spritesheet.png`(64×64 타일 4×4 그리드 - 정확한 타일 배치는 `assets/snake/snake_spritesheet_guide.md`/`assets/snake/README.md` 참고, 이미 한 번 걸렸던 방향 규칙 함정 포함: 꼬리 타일의 "방향"은 "꼬리가 시각적으로 가리키는 방향"이 아니라 "꼬리에서 그 바로 앞 칸(머리 쪽 이웃)으로 가는 방향"이고, 이걸 거꾸로 하면 남/북 꼬리 타일이 조용히 뒤바뀝니다)를 로드합니다. `render/layers.js`의 `snakeLayer`는 `getSnakeSpriteSheet()`가 로딩을 마쳤으면 `spriteSnakeLayer`를, 아니면 `fallbackSnakeLayer`(원래의 색깔 사각형 렌더러, 아트 파일이 아직 없을 때나 헤드리스 테스트 환경에서 `Image`의 `load`가 절대 안 일어날 때도 게임이 의도된 것처럼 보이게 하려고 삭제하지 않고 남겨둠)를 고릅니다.
- **이동은 틱당 한 칸씩 딱딱 움직이는 이산적 방식입니다 - 틱 사이 보간이 아니고, 카메라만 부드럽게 따라갑니다.** 이전 버전은 이전 틱과 현재 틱 사이 각 칸의 픽셀 위치를 보간(`tickProgress` 비율, `Snake`의 `prevSegments`/`prevDir`)해서 어떤 프레임률에서도 부드럽게 보이게 했습니다. 이건 되돌려졌습니다: 회전하는 동안 각 칸을 독립적으로 선형 보간하면 머리와 그 바로 뒤 목 칸이 서로 다른 축을 따라 동시에 움직이게 되는데, 이건 보간을 하는 이상 기하학적으로 피할 수 없는 결과이고, 머리가 고정된 방향의 스프라이트가 된 순간부터 눈에 띄게 어긋나 보였습니다(색깔 사각형은 "잘못된 방향"이라는 게 없지만, 스프라이트는 있습니다). 타이밍을 아무리 조정해도(머리 스프라이트 방향 전환을 늦추는 등) 완전히 고쳐지지 않아서, 보간 자체를 통째로 없앴습니다 - `Snake.step()`은 그냥 `segments`를 틱당 한 번 제자리에서 바꾸고, 스프라이트 타일 결정도 소수점 보간 없이 `snake.dir`을 그대로 읽습니다. 카메라 부드러움(위)은 영향받지 않고, 의도적으로 유일하게 순간이동이 아니라 부드럽게 움직이는 요소로 남아있습니다 - 미완성이거나 놓친 게 아니라 의도된 설계입니다.

**상태창은 완전히 별도인 세 번째 캔버스입니다 - 이제 하단 바가 아니라 좌측 사이드바입니다.** (`#status-canvas`, `main.js`에서 고정 `STATUS_PANEL_WIDTH`(240) 너비 × 게임 캔버스와 같은 높이로 설정 - 두 캔버스가 나란히 붙어 하나의 프레임처럼 보이려면 높이가 일치해야 해서, `main.js`는 별도 상수가 아니라 `getViewportPixelSize()`에서 상태창 높이를 뽑습니다.) `render/statusPanel.js`의 `createStatusPanel()`은 네 구획을 위에서 아래로 쌓습니다(예전엔 게임 캔버스 아래에 좌우로 나열된 열이었다가, 레이아웃이 좌측으로 옮겨가면서 내부 배치도 그에 맞춰 바뀌었습니다): 맨 위 **점수+미니맵**(어두운 패널 위 골드색 실시간 총점, 그 아래 `render/minimap.js`의 미니맵), **STATUS**(속도, 현재 길이 - `drawSnakeHeadIcon`으로 별도 아이콘 파일이 아니라 뱀 스프라이트시트의 동쪽을 보는 머리 타일을 그대로 잘라와서 보여주므로 길이 아이콘이 항상 실제 뱀 모습과 일치함 - 그리고 무기가 풀린 뒤엔 독침/비늘파동 데미지), **KILL STACK**(적 타입별 이번 판 킬 집계, `world.stats.killsByType`), **CINTAMANI**(색상별 네 개 수량 - 아래 "cintamani 스킬 메커닉" 참고). 아래 세 구획은 남은 높이를 균등하게 나누고 각각 공유 `drawIconOrFallback` 헬퍼로 아이콘-위/수치-아래 슬롯을 그립니다. `setMerged(bool)`/`renderBlank()`/`renderDimOverlay()`는 예전과 정신적으로 같습니다 - 내부 구획 배치와 방향만 바뀌었을 뿐, `game.js`/각 상태와의 외부 계약은 그대로입니다.
- **`world.stats.killsByType`**(`{ [적타입id]: 처치수 }`)는 기존 `enemyKillStacks`와 별개의 카운터입니다 - 혼동하지 마세요. `enemyKillStacks`는 일부 `onDefeated` 훅만 선택적으로 쓰는 보상 트리거 배관이고, `killsByType`은 모든 킬 경로(투사체, 포획, cintamani 스킬 - 아래 참고)에서 무조건 증가하며, 상태창에 보여줄 범용 데이터와 cintamani 해금/보상 판정이 폴링할 균일한 신호를 만드는 게 목적입니다. `world.reset()`마다 리셋됩니다(이번 판 한정, 판을 넘는 영구 누적 아님).
- **`world.stats.cintamani`**(`{ red, blue, green, yellow }`)는 CINTAMANI 구획의 실시간 데이터입니다 - 어떻게 얻고 쓰는지는 더 아래 "cintamani 스킬 메커닉" 참고. `green`/`yellow`는 `content/cintamani/index.js`에 아직 red/blue만 등록돼 있어서 항상 0입니다.
- **아이콘은 선택적이고 방향 무관한 이미지이며 색깔-도형 폴백이 있습니다** - 뱀 스프라이트시트와 같은 점진적 대체 관례입니다(위 "뱀 스프라이트 렌더링" 참고): `render/statusIcons.js`가 `assets/enemies/enemy{1..5}.png`와 `assets/cintamani/{red,blue,green,yellow}.png`를 모듈 로드 시 한 번 불러오고(헤드리스 테스트 안전을 위해 `typeof Image !== 'undefined'` 가드), 파일이 있고 로딩까지 끝나야만 `getEnemyIcon`/`getCintamaniIcon`이 값을 돌려줍니다. `assets/enemies/README.md`/`assets/cintamani/README.md`가 그림 템플릿(정확한 파일명, 정사각형 PNG, 투명 배경, 64×64 권장)이고, 사용자가 손으로 그립니다.
- **`getEnemyIcon(id)`는 상태창 전용이 아닙니다 - `managers/enemyManager.js`의 실제 필드 렌더링도 같이 씁니다**, 그래서 같은 파일 하나가 킬스택 아이콘과 필드 위 적 모습을 동시에 바꿉니다. 필드 렌더링은 이미지가 있든 없든 HP 숫자(`displayText`)를 위에 계속 그립니다.
- **`statusIcons.js`의 `drawIcon`은 손그림 아이콘의 투명 여백을 자동으로 잘라내고 그립니다.** 손그림 64×64 PNG가 여백을 남겨서 폴백 도형보다 작아 보이는 실제 문제 때문에, 로드 시점에 투명하지 않은 픽셀의 경계 상자를 계산해 `WeakMap`에 저장해두고, 있으면 그 영역만 잘라서 목표 박스 전체를 채웁니다.
- 가죽 질감 배경(밝은 갈색 바탕 + 반투명 어두운 대각선 + 구획 구분선)은 매 `render()`마다 새로 그립니다 - 캐싱할 만큼 비싸지 않습니다.
- **두 캔버스는 `title`/`testBuildSelect`/`leaderboardView`에서는 하나로 합쳐 보이고, `playing`/`gameOver`에서는 다시 나뉩니다.** `#play-area.merged` 클래스가 간격/경계선을 없애 하나의 프레임처럼 보이게 합니다. `titleState`/`testBuildSelectState`/`leaderboardViewState`가 `setMerged(true)`, `playingState`/`gameOverState`가 `setMerged(false)`를 호출합니다. 합쳐진 동안 `statusPanel.renderBlank()`가 게임 캔버스 배경색과 정확히 같은 색으로 상태창 캔버스를 채워서, 이어붙인 화면이 하나로 보이게 합니다.

**히트 피드백(파티클 + 머리 반짝임)**: 처치/픽업이 밋밋하게 느껴지지 않도록 추가한 가벼운 시각 효과 두 가지 - 게임 상태에 영향 없으므로, 게임플레이 로직만 추적한다면 건너뛰어도 안전합니다.
- `entities/particle.js` + `managers/particleManager.js`는 발사체와 같은 모양이지만 충돌 판정 대신 `life`/`maxLife`를 갖습니다. `spawnBurst({x,y,color,count,speed,life})`가 원형으로 고르게 퍼지는 파티클을 만드는 유일한 방법입니다.
- 처치 지점(플레이어 투사체 킬, 포위 포획 킬, `content/cintamani/shared.js`의 `killEnemyForCintamani` - 더 아래 참고)마다 처치된 적 자신의 색으로 버스트를 직접 동기 호출합니다 - `eventBus`를 거치지 않습니다.
- **아이템 픽업 반짝임은 머리→꼬리로 흘러가는 파동이지, 단순 머리 깜빡임이 아닙니다** - `Snake` 인스턴스 상태(`flashColor`, `flashElapsedMs`)입니다. 각 칸 `i`는 `i * ITEM_FLASH_STAGGER_MS`에 시작해 `ITEM_FLASH_DURATION_MS` 동안 지속되는 자기만의 반짝임 창을 갖고, `snake.updateFlash(dt)`는 `onFrame`에서 매 프레임 호출돼 실제 시계 기준으로 파동 속도가 일정합니다.

**실시간 점수** (`core/score.js`, `config/constants.js`의 `SCORE_PER_*`): 리더보드가 원래 순수 생존시간만으로 순위를 매겨서, 뱀을 키우는 게 위험만 늘리고 점수엔 도움이 안 되는 - 즉 "그냥 안 먹고 계속 피하기만 하는 게 최적"인 상황을 낳았습니다. 해결책은 실제로 뭔가를 이뤄낸 순간마다 쌓이는 값 + 길이/속도 기반 가산점입니다:
- 한 덩어리 총점 대신, `world.stats`는 `survivalScore`/`killScore`/`itemScore` 세 개의 순수 가산 요소를 따로 추적합니다 - 각각 그 일이 일어나는 순간 `+=`됩니다. 생존 점수는 `onFrame`마다 조금씩(화면에서 계속 부드럽게 올라가 보이게), 아이템 점수는 섭취 시점에, 처치 점수는 모든 킬 지점(투사체/포획/cintamani 스킬)에서 - 포획이 4배 더 후한 이유는 아래 밸런스 항목 참고. 세 요소를 나눈 이유는 리더보드 이름 입력 화면이 총점만이 아니라 "어디서 왔는지"도 보여주기 위해서입니다.
- **점수 가중치는 짐작이 아니라 헤드리스 시뮬레이션으로 조정했습니다.** "위험 회피 + 가까운 먹이로 이동 + 정렬되면 발사"하는 단순 봇으로 실제 게임 모듈들을 써서 약 200판을 시뮬레이션했고, 최종 점수 중 각 요소가 차지하는 비율을 집계했습니다. 이 봇은 (일부러 포획 로직 없이) 포획 킬을 200판 중 0번, 의미 있는 속도 가산점도 평균 0.1%만 얻었습니다 - 둘 다 사람이 이 게임에서 해내기 가장 어려운 일이기 때문입니다. 튜닝 전 가중치가 이걸 사소한 행동과 거의 같은 값으로 쳐주고 있었던 걸 근거로, 포획 처치 점수를 투사체 처치의 4배로, 속도 가산점을 5배로 올렸습니다.
- `world.stats.maxLength`/`maxSpeedLevel`은 이번 판에서 도달한 최고치를 추적하고 - 줄어들어도(예: `speedDown`) 안 깎입니다. `core/score.js`의 `getTotalScore(world)`가 표시/최종 점수의 유일한 기준이고, 가산점 항목은 매번 처음부터 다시 계산해서(누적값이 아니라) 절대 중복 계산되거나 어긋나지 않습니다.
- 표시는 DOM이고 캔버스 밖에 있습니다 - 원래 상단 스탯 바에 있었는데, 좌측 사이드바 개편 이후 상태창 맨 위에도 실시간 총점이 표시됩니다. 둘 다 `getTotalScore(world)`를 각자 독립적으로 호출하므로 자동으로 일치합니다.
- **점수 팝업**("+20" 식으로 아이템/처치 지점에 뜨는 것)은 DOM 총점과 무관하게 여전히 캔버스에 그립니다 - 세계 좌표에 묶여있는 값이라서입니다.
- **리더보드는 이 계산된 점수로 순위를 매깁니다 - 원시 생존시간이 아니라.** 마이그레이션 세부사항은 아래 리더보드 항목 참고.

**속도 표시에는 실제 속도 잠금 해제/점수 계산과는 별개의 화면 전용 보정값이 있습니다** (`core/speedLevel.js`, `config/constants.js`의 `SPEED_LEVEL_DISPLAY_OFFSET`). `getSpeedLevel(tickMs)`가 유일한 원시 계산이고, `getDisplaySpeedLevel(tickMs)`가 화면에 보여줄 값에만 4를 더합니다(새 판이 "16" 대신 "20"으로 보이게). 이 보정은 점수 계산에는 일부러 안 들어갑니다 - 점수 가산점은 항상 차이값이라 양쪽에 같은 상수를 더해도 어차피 상쇄되므로, 화면 표시 쪽에만 두는 게 더 정확하고 변경도 더 작습니다.

**리더보드** (`api/leaderboard.php`, `js/services/leaderboardApi.js`, `js/render/leaderboardPanel.js`, `states/gameOverState.js`에 연결): 이 코드베이스 최초의 서버 코드, 최초의 네트워크 I/O, 최초의 실제 HTML `<input>` UI입니다.

- 백엔드는 PHP 파일 하나(`api/leaderboard.php`)로, `GET`(상위 기록 반환)과 `POST`(`{name, survivalMs, score}` → 검증 → 추가 → 재정렬 → 상한 자르기 → 저장)를 `api/data/leaderboard.json`(`flock()` 보호, DB 없음)에 처리합니다. 진지한 부정행위 방지는 없습니다 - 캐주얼한 가정용 NAS 리더보드일 뿐이라, 이름/길이 제한과 `score`/`survivalMs`가 유한한 양수인지 정도만 검사합니다. `api/data/*.json`은 gitignore 대상입니다.
- `js/services/leaderboardApi.js`는 `fetch()`를 감싸고 **절대 throw하지 않습니다** - 네트워크/API 실패는 `{ ok: false, error }`를 돌려줄 뿐이라, 리더보드 API가 아직 배포 전이거나 NAS가 잠깐 안 되도 게임오버 화면 자체는 계속 동작합니다.
- `js/render/leaderboardPanel.js`는 진짜 `<input>`(`#leaderboard-name-input`) 기반의 첫 DOM 컨트롤러입니다 - 한글 IME 이름 입력에는 진짜 `<input>`이 필수라서입니다.
- **이름 입력 화면은 최종 점수를 구성 요소별로 보여줍니다.** `die()`가 `getScoreBreakdown(world)`를 `'playerDied'` 이벤트 payload에 스냅샷으로 담고, `leaderboardPanel.js`가 총점(굵고 골드) + 구성요소 목록 + 가산점 세부 목록 세 단으로 나눠 그립니다.
- **순위는 `survivalMs`가 아니라 계산된 `score` 기준입니다.** 이 시스템 이전의 기존 기록(`score` 필드 없음)은 읽을 때 `score = round(survivalMs/1000 * LEGACY_SCORE_PER_SECOND)`로 지연 계산됩니다 - "생존 점수만 있고 가산점은 0"으로 취급하는 셈입니다. `LEGACY_SCORE_PER_SECOND`는 PHP 쪽 상수로, `config/constants.js`의 `SCORE_PER_SECOND`와 사람이 직접 맞춰야 합니다(JS/PHP 사이 공유 소스 없음). `leaderboardPanel.js`의 한 줄짜리 표시는 `formatScore(entry.score)`를 씁니다 - 압축된 목록에는 이제 생존시간이 안 보이고, 방금 끝난 내 판의 상세 내역 화면에서만 보입니다.
- **`game.js`는 `leaderboardPanel`을 딱 하나만 만들어서 `gameOverState`와 `states/leaderboardViewState.js`가 같이 씁니다** - `statusPanel`과 같은 "한 번만 생성" 패턴입니다. DOM 엘리먼트가 싱글턴이라, 두 번 만들면 폼의 `submit`/건너뛰기 버튼 `click` 리스너가 중복 등록됩니다. `setHandlers({ onSubmit, onSkip })`로 `gameOverState`가 판별 핸들러를 생성 시점 없이 붙일 수 있고, `leaderboardViewState`는 아예 호출하지 않습니다(폼 자체를 항상 숨기는 `panel.showResultPhase()`만 호출).
- **`states/leaderboardViewState.js`**는 타이틀 화면에서 `'l'`로 들어가는, 판 시작 없이 순위만 보는 화면입니다. `panel.showResultPhase(entries, -1)`을 그대로 재사용하고, `Enter`로 `title`로 돌아갑니다.
- `gameOverState.enter(payload)`는 `async`입니다: `fetchLeaderboard()`를 바로 시작하면서 패널의 **입력 단계**(이름 입력창에 포커스)를 즉시 보여줍니다. 이때 일부러 `Actions.bind('Enter', onRestart)`를 아직 걸지 않습니다 — 이름 `<input>`에 입력한 뒤 등록하려고 누르는 Enter를 가로채면 안 되기 때문입니다. 사용자가 등록/건너뛰기 하면 **결과 단계**(`finish()`)로 넘어가고 그제서야 Enter를 바인딩합니다. `exit()`는 Enter를 해제하고 패널을 숨깁니다.
- **테스트 모드 판은 아예 리더보드로 안 갑니다.** `world.testMode`가 참이면 `gameOverState.enter()`는 Enter 바인딩만 하고 바로 리턴합니다 — 테스트 모드가 인위적으로 부풀린 길이/속도로 시작하므로, 그 기록이 진짜 판과 섞이면 순위표가 무의미해집니다. (새 빌드 선택 화면으로 고른 어떤 테스트 빌드든 내부적으로 여전히 `testMode: true`라 이 규칙을 그대로 물려받습니다.)
- 리더보드 `GET`/`POST` 응답은 비동기라, 응답 전에 플레이어가 건너뛰기/빠른 등록을 할 수 있는 실제 경쟁 상태를 막기 위한 `generation`/`phase` 가드가 있습니다. `leaderboardViewState.js`도 같은 이유의 `generation` 가드를 갖습니다.

**Cintamani(여의주)는 몸 모양으로 발동하는, 레지스트리 기반 스킬 시스템입니다** (`content/cintamani/`, 위의 `content/enemies|items|mechanics`와 같은 확장 패턴 - 순수 def 파일 + `index.js` 하나가 각각 import해서 등록하므로, 새 색상은 파일 하나 + 등록 한 줄이면 되고 `states/playingState.js`나 상태창은 절대 안 바뀝니다). def(오늘은 `red.js`, `blue.js`; `green`은 자리만 예약, 아래 참고)는 `{ id, requiredKills: {적id: 수}, rewardEnemyId, rewardInterval, pattern, activate(world) }` 형태입니다.
- **해금, 보상 적립, 발동 전부 하나의 폴링 함수 `updateCintamani(world)`가 담당합니다**(`states/playingState.js`, 틱마다 한 번, 직접 테스트할 수 있도록 export도 됨) - 개별 처치 지점마다 훅을 거는 대신, 매 틱 `world.stats.killsByType` 스냅샷에서 모든 걸 다시 계산합니다. 그래서 처치가 *어떻게* 일어났든(투사체, 포획, cintamani 스킬 자신의 처치) 상관없이 다음 틱에 자동으로 반영되고, `content/cintamani/*.js` def 파일들이 처치 지점을 직접 알 필요가 없습니다(레지스트리로의 순환 참조도 피함). 색상은 `requiredKills`에 있는 모든 적 id를 충분히 잡아야 해금되고, 해금되는 바로 그 틱에 `rewardEnemyId`의 현재 킬 수를 `cintamaniRewardBaseline`으로 저장해서 해금시킨 그 킬 자체가 첫 보상에 안 겹치게 합니다. 그 뒤로는 `rewardEnemyId`를 `rewardInterval`만큼 더 잡을 때마다 여의주 하나가 늘어납니다(`world.stats.cintamani[id]++`), 나눗셈으로 계산해서 한 틱에 여러 조건 충족 킬이 몰려도 정확한 개수가 지급됩니다.
- **발동은 키 입력이 아니라 몸 모양 인식입니다.** 각 def의 `pattern`은 머리 주변 어떤 칸이 뱀 몸으로 채워져 있어야 하는지 나타내는 작은 ASCII 격자(`algorithms/patternMatch.js`의 `parsePattern`, 머리가 동쪽을 보는 걸 기준으로 그려짐)이고, `matchesPattern`이 이걸 뱀의 *현재* 진행 방향에 맞춰 회전시켜 대조합니다. 포위 포획 메커닉처럼, 발동은 모양이 *막 갖춰지는* 그 틱에만(`cintamaniPatternMatched[id]` 전환 감지) 일어나고, 여의주 재고가 1개 이상 있어야 합니다 - 성공하면 정확히 하나를 소비하고 `def.activate(world)`를 부릅니다. `red`의 패턴은 뱀이 보는 방향으로 필드 끝까지 뻗는 관통 빔(폭 3칸, 투사체 면역 적까지 강제로 파괴, 독침/비늘파동과 다름)을 쏘고, `blue`는 다른 형태의 범위 효과입니다(`content/cintamani/blue.js` 참고). 둘 다 `content/cintamani/shared.js`의 `killEnemyForCintamani(world, enemy, scoreAmount)`를 통해 처치를 처리하는데, 이건 `playingState.js`의 `grantKillReward`, `projectileManager.js`의 자체 사본과 마찬가지로 "제거 + onDefeated + 파티클 + 점수/팝업 + killsByType" 묶음의 또 다른 사본입니다 - 모듈 경계를 넘어 공유하기보다 각자 갖는 게 낫다고 판단한 같은 전례를 따릅니다.
- **디버그 키 `1`/`2`/`3`(테스트 모드 전용)은 해금/패턴/재고 확인을 전부 건너뛰고 해당 색상의 `activate()`를 직접 발동시킵니다**(`playingState.js`의 `triggerCintamaniDebug`) - 실제로 여의주를 모으고 정확한 몸 모양을 그리지 않고도 시각/데미지 효과를 빠르게 확인하기 위해서입니다. `green`은 `3`에 배정돼 있지만 아직 등록된 def가 없어서 지금은 눌러도 아무 일도 안 일어납니다 - green이 디자인될 때를 위한 예약 자리로 남겨뒀습니다. 테스트 빌드 선택 화면(아래)에서는 이 숫자키 세 개가 완전히 다른 의미(빌드 1/2/3 고르기)를 갖습니다 - "테스트 모드 빌드 선택" 참고.

**터치 컨트롤은 터치 우선 기기에서만 보이는, 키보드 이벤트를 흉내내는 투명 DOM 오버레이입니다** (`input/touchControls.js`). `isTouchPrimaryDevice()`는 브라우저/기기 문자열이 계속 바뀌어 깨지기 쉬운 User-Agent 파싱 대신 `(pointer: coarse)`(안 되면 `ontouchstart` 존재 여부)를 봅니다. 화면 위 각 버튼(`KEY_MAP`)은 `touchstart`/`touchend`/`touchcancel`에서 진짜 키를 눌렀을 때와 똑같은 `key`/`code`로 `KeyboardEvent`를 발사하므로, 코드베이스의 기존 키보드 리스너(`input/actions.js`, `input/input.js`, `playingState.js`의 raw 스페이스 리스너) 전부가 터치 전용 분기 없이 그대로 받아 처리합니다. `setTouchActionButtons(visibleIds)`는 각 상태의 `enter()`에서 한 번씩 호출돼 그 화면에 의미 있는 버튼만 보여줍니다 - 예를 들어 `testBuildSelectState`는 `touch-1`/`touch-2`/`touch-3`을(인게임 cintamani 디버그 의미에서 그 화면에서만 "N번 빌드 고르기"로 의미가 바뀜) 보여주고, `playingState`는 방향패드 + 스페이스/P, 테스트 모드에서는 원래 의미의 1/2/3 버튼도 같이 보여줍니다. 방향패드(D-pad)는 상태와 무관하게 항상 표시됩니다.

**패치 노트는 타이틀 화면에 보이는, 플레이어 대상의 계속 추가되는 로그입니다** (`config/patchNotes.js`의 `PATCH_NOTES` 배열, `render/patchNotesPanel.js`의 DOM 스크롤 패널, `titleState.js`가 enter/exit에서 표시/숨김). 각 항목은 `{version, datetime, visible, lines}`이고, git 태그와 정확히 맞을 필요 없는 자유 형식 표시 문자열입니다 - 플레이어가 알아챌 만한 변경마다 배열 맨 위에 새 항목을 추가하세요(위 "브랜치·버전 워크플로우"의 당부 참고). `visible: false`는 배열에서 지우지 않고 패널에서만 숨깁니다.
- **`core/updateCheck.js`는 NAS에 새 코드가 올라오면 대기 중인 타이틀 화면 탭을 자동으로 새로고침합니다** - 이미 로딩된 페이지는 새 배포가 있었는지 달리 알 방법이 없기 때문입니다. `index.html` 자체를 다시 받아와서(`cache: 'no-store'`, 이 요청마저 브라우저 캐시에서 옛날 걸 받으면 비교 자체가 무의미해지므로) `main.js?v=...` 문자열을 추출하고, 지금 실행 중인 `BUILD_VERSION`과 다르면 새로고침합니다. `titleState.js`만 이걸 시작/정지시킵니다 - 플레이 중에 절대 돌면 안 되므로(배포가 뜨는 순간 진행 중인 판이 날아갈 수 있음).

**테스트 모드 빌드 선택**: 타이틀 화면에서 `t`를 누르면 곧바로 테스트 모드를 시작하는 대신 빌드 선택 화면이 뜹니다(`states/testBuildSelectState.js`, 2026-08-03 추가 - `core/stateMachine.js`의 `testBuildSelect` 상태). `config/testBuilds.js`의 `TEST_BUILDS` 배열(`PATCH_NOTES`와 같은 "평평한 배열, 항목 추가로 확장" 모양)이 고를 수 있는 실험들을 나열합니다 - 각각 `{id, version, description, statsOverrides, viewportOverrides}`이고, 이 화면에서 숫자키(`1`/`2`/`3`...)로 고릅니다; `Escape`는 `title`로 돌아갑니다. 하나를 고르면 `startGame({testMode: true, buildId: build.id})`를 부르는데, 이건 `game.js`의 기존 `startGame`이 그대로이고 `buildId`만 선택적으로 추가로 실립니다.
- **모든 빌드는 여전히 똑같은, 안 바뀐 테스트 모드 기본값을 그대로 갖습니다**(`TEST_MODE_INITIAL_LENGTH`, `TEST_MODE_TICK_MS`, 미리 채워진 chaser 킬 스택과 cintamani 재고, `1`/`2`/`3` 디버그 키) - 빌드의 `statsOverrides`/`viewportOverrides`는 이 기본값 대신이 아니라 그 위에 덧씌워집니다. 이건 명시적 요구사항이었습니다: 빌드는 밸런스 변경(생성 속도, 시야 등)을 시험하기 위한 것이지, 테스트 모드 자체의 "게이트로 막힌 콘텐츠를 빠르게 시험한다"는 설정을 매번 다시 만드는 게 아닙니다.
- **`statsOverrides`는 `game.js`의 `world.reset()` 안에서 다른 모든 기본 필드가 다 설정된 뒤 맨 마지막에 `world.stats`에 펼쳐집니다** - 그래서 빌드는 `world.stats`에 이미 있는 어떤 이번 판 값이든(위 `World` 객체 항목 참고) 이름만 대면 덮어쓸 수 있고, 다른 곳에 별도 배관이 필요 없습니다. 오늘 실제로 연결된 예시는 `enemySpawnMinMs`/`enemySpawnMaxMs`입니다(`managers/enemyManager.js`의 `_randomSpawnDelay(world)`가 이제 이걸 `world.stats`에서 읽고, `world`가 아직 없을 때만(즉 `world.stats`가 생기기 전인 최초 스폰 타이머 하나만) `config/constants.js` 기본값으로 대체합니다). `statsOverrides`/`viewportOverrides`가 비어있는 `baseline` 항목이 항상 "실험 없음" 대조군으로 목록에 남아있습니다.
- **`viewportOverrides`는 `world.stats`가 아니라 `core/viewportConfig.js`를 거칩니다** - 이유는 위 "카메라·뷰포트" 참고, 그리고 오버라이드가 고정된 캔버스 픽셀 크기를 반드시 유지해야 한다는 제약도 마찬가지입니다. `game.js`의 `world.reset()`은 `world.camera`를 만들기 *전에* `setViewportConfig(build.viewportOverrides)`를 호출해서, 카메라의 초기 중앙 정렬이 이미 오버라이드된 시야 기준으로 계산되게 합니다. 일반 판(또는 `baseline` 빌드 테스트 판)은 대신 `resetViewportConfig()`를 호출해서, 이전 판의 실험적 시야가 다음 판에 남아있지 않게 합니다.
- **이건 두 사람 협업 워크플로우의 "실험실"이기도 합니다** - 위 "브랜치·버전 워크플로우" 참고: 실험은 `master`에 merge돼 NAS에 실제로 배포되면서도, 이 테스트 모드 화면을 통해서만 도달할 수 있으므로 일반 플레이어에게는 완전히 안 보일 수 있습니다. 그래서 (배포된 빌드만 플레이할 수 있는 협업자 때문에 필요한) 아직 결정 안 된 밸런스 아이디어를 실제 운영 환경에 올려도, 아무 플레이어의 실제 게임에도 영향을 안 주는 게 가능해집니다. 어떤 빌드를 확정한다는 건 그 `statsOverrides`/`viewportOverrides`를 `testBuilds.js`에서 꺼내 진짜 기본 동작으로 승격시키는 것 - 그 자체로 별도의, 의도적인 후속 변경이지, 그 빌드가 `master`에 배포됐다고 자동으로 일어나는 일이 아닙니다.
