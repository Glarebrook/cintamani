# Cintamani

NAS에서 서빙하는 브라우저 웹게임.

## 실행 방법

NAS 웹 서버(예: Synology Web Station, NGINX) 루트에 이 폴더를 배치한 뒤 `index.html`을 서빙하면 됩니다.  
별도 빌드 과정 없이 정적 파일만으로 동작합니다.

## 폴더 구조 (V2)

ES 모듈(`<script type="module">`)로 구성되어 있으며, 여전히 빌드 과정 없이 정적 파일로 동작합니다.

```
/
├── index.html                 진입점 — <script type="module" src="js/main.js">
├── css/style.css               전역 스타일
└── js/
    ├── main.js                 초기화 진입점 — 캔버스 크기 설정, Game 생성/시작
    ├── game.js                 최상위 오케스트레이터 — World 생성, GameLoop/StateMachine 연결
    ├── config/constants.js     게임 상수 (CELL_SIZE, GRID, 타이밍 등)
    ├── core/                   loop(고정 틱 루프), stateMachine, eventBus, registry, gridMath
    ├── input/                  input.js(이동 입력), actions.js(재시작/발사 키 바인딩)
    ├── entities/                snake.js, projectile.js
    ├── content/                 확장 지점 — 적/아이템/포획류 메커니즘을 새 파일 + 등록 한 줄로 추가
    │   ├── enemies/              basic.js(적1), sentinel.js(적2, 포획존 opt-in), index.js(레지스트리)
    │   ├── items/                 food.js, index.js
    │   └── mechanics/             encirclement.js(포획 flood fill 메커니즘), index.js
    ├── algorithms/floodFill.js  순수 BFS 알고리즘 (게임 지식 없음)
    ├── managers/                 enemyManager.js, itemManager.js, projectileManager.js
    ├── systems/collision.js     벽/자기충돌(+포획 예외)/적충돌/먹이 판정 — 순서가 고정된 핵심 로직
    ├── render/                   renderer.js, layers.js, hud.js, overlays.js
    └── states/                   playingState.js, gameOverState.js
```

---

## 개발 워크플로우 (dev → master)

기획자(사용자)가 계속 새 기능을 시험해보고, 마음에 든 것만 골라 정식 버전에 반영하는 방식으로 운영합니다.

- **`master` 브랜치 = 운영(안정) 버전.** 항상 실제로 서빙해도 되는 상태로 유지합니다.
- **`dev` 브랜치 = 실험 공간.** 새 기능은 전부 여기서 만들고 테스트합니다. 하나의 `dev` 브랜치를 계속 재사용합니다(기능마다 새 브랜치를 파지 않음).
- 기능이 확정되면: `dev`를 `master`에 merge → 아래 변경 이력에 새 버전 한 줄 추가 → 그 커밋에 git 태그(`v2.1.0`처럼)를 남겨서 "이 시점 = 이 버전"을 고정해둡니다. 문제가 생기면 태그로 언제든 돌아갈 수 있습니다.
- 캐시 무효화용 `?v=` 문자열과 화면 우측 하단에 보이는 빌드 버전(`BUILD_VERSION`)은 `dev`에서 코드를 바꿀 때마다 매번 같이 올립니다 — 새로고침했을 때 화면의 버전 숫자가 바뀌어 있으면 최신 코드가 반영된 것, 안 바뀌어 있으면 브라우저 캐시 문제라는 걸 바로 확인할 수 있습니다.
- NAS 배포(운영 반영)는 별도 단계입니다 — 어떤 방식으로 NAS에 올리고 계신지에 따라 "merge = 곧바로 운영 반영"인지 "merge 후 수동으로 한 번 더 올려야 하는지"가 달라지므로, 확인되면 이 섹션에 구체적인 절차를 추가합니다.

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 0.1.0 | 2026-05-02 | 프로젝트 초기 세팅 — 기본 캔버스 루프, 폴더 구조 구성 |
| 0.2.0 | 2026-05-02 | 스네이크 기본 구현 — 필드(1000×500), 뱀 이동(0.4초/칸), 먹이 스폰(2초), 먹이 접촉 시 몸 성장(먹이 위치를 뱀이 완전히 지난 후 추가), 벽/자기충돌 게임오버, Enter 재시작 |
| 2.0.0 | 2026-07-27 | 전면 모듈화 재작성 — 전역 스크립트(8개 파일) → ES 모듈(레지스트리/시스템/상태머신 기반, 약 30개 파일)로 전환. 기존 동작(이동/성장/충돌/투사체/적 2종/포획존 flood-fill 메커니즘)은 그대로 유지하면서, 새 적/아이템/포획류 메커니즘을 충돌·렌더 코드를 건드리지 않고 추가할 수 있는 구조로 재구성. 구버전 전체는 `V1/`에 원본 그대로 보관 |
| 2.1.0 | 2026-07-27 | 포획존 메커니즘 버그 수정 4건 — (1) 포획 직후 자기충돌 통과가 한 틱만 유지되던 것을 몸에서 완전히 벗어날 때까지 유지되도록, (2) 먹이를 연달아 먹으면 이전 성장이 사라지던 것을 여러 개를 동시에 기다리는 대기열로, (3) 포획에 쓰이는 고리가 회색 캡처존 밖으로 나가도 인정되던 것을 고리 전체가 캡처존 안에 있어야 하도록, (4) 우연히 위상적으로 막힌 구역을 머리가 나중에 그냥 지나가기만 해도 포획되던 것을 "막 닫히는 순간"에만 포획되도록 수정. 그 외 적 충돌 판정을 정중앙 1칸이 아닌 그려지는 3×3 박스 전체로 확장, 적 최대 생성 수 제한(3마리) 제거 |
| 2.2.0 | 2026-07-27 | dev/master 브랜치 워크플로우 도입 후 첫 반영 — 발사체가 뱀 몸통(머리 제외)에 닿으면 소멸하도록 수정, 파란 적 회색 포획 범위를 반투명 원형 그라데이션으로 변경(가장자리가 부드럽게 사라짐), 화면 우측 하단에 현재 빌드 버전 표시 추가, 뱀 초기 크기를 3으로 복귀(테스트용으로 40까지 늘렸던 것), 먹이가 동시에 최대 10개까지 생성되도록 변경(기존엔 1개뿐이었음) |
| 2.3.0 | 2026-07-29 | 타이틀 화면 추가(CINTAMANI 타이틀 + 아무 키나 눌러 시작, 게임오버 후 Enter는 타이틀로 복귀) / 먹이를 5종류로 확장(초록=기본, 노랑=성장+가속, 갈색=축소+감속, 보라=3배 성장, 주황=공격력 증가) / 새 적 2종 추가 — 3번(추격형: 평소 배회, 감지범위 진입 시 추격+가속, 처치 5스택마다 보라 먹이 보상)과 4번(고정 포탑형: 뱀과 같은 행/열에 정렬되면 투사체 발사, 포획 처치 시 10% 확률로 주황 먹이 보상) / 화면 통계에서 "게임속도" 항목 제거, "뱀속도"를 체감하기 쉬운 단계 표시로 변경 / 기본 이동 간격·투사체 속도 밸런스 조정 |
| 2.4.0 | 2026-07-29 | 5번적(추적 사격형) 추가 — 뱀과 같은 속도로 접근, 정렬 진입 시 3발 버스트 발사, 몸 피격 시 길이+속도 감소 / 아이템 보상 트리거 재배정 및 무작위 스폰을 초록 먹이 하나로 제한(간격 3~7초, 최대 5개) / 적 동시 존재 최대 3마리 제한, 2·4번적 스폰 조건 통일(뱀 길이 20 이상), 5번적은 뱀 속도 80ms 미만에서만 스폰 / 이동형 적(3·5번)이 뱀 틱 사이에 머리를 지나가며 충돌 판정을 피하던 버그 수정 / 타이틀에 테스트 모드(T, 길이 40·속도 70ms) 추가 / 포획형 적(2·4번)이 맵 가장자리에 붙어 생성돼 영구히 포획 불가능해지던 버그 수정 / 구버전 V1 폴더 삭제(git 히스토리로 복구 가능) / **리더보드 기능 추가** — 게임오버 시 이름(최대 200자)을 등록하면 생존시간 기준 공유 순위표에 남고, 타이틀에서 L로 플레이 없이 순위표만 열람 가능, 각 기록에 등록 일시 표시, 테스트 모드 기록은 리더보드에서 제외. 백엔드는 NAS의 PHP로 구현(`api/leaderboard.php`) |
| 2.5.0 | 2026-07-29 | 적이 뱀 머리로부터 8칸 이상 떨어진 곳에만 생성되도록 수정(너무 가까이 생성돼 반응할 틈 없이 죽는 문제 방지) / 3번적(추격형) 평상시 배회를 완전 무작위에서 방향 유지형(3~8걸음 한 방향 유지 + 가끔 지터)으로 변경 / 3·5번적 이동 속도 150ms로 감속, 4·5번적 투사체 속도를 기존의 1/8로 감속 / 리더보드 UI 개선 — 빈 이름으로 제출 시 건너뛰기와 동일하게 처리, 이번 판 기록을 결과 화면에 강조 표시, 좌(이번 기록/이름 입력)·우(기존 순위표) 2단 레이아웃, 순위표 한 줄당 이름·기록은 크게·타임스탬프는 작게 표시 / 타격감 추가 — 적 처치·포획 시 파티클 버스트, 아이템 섭취 시 머리부터 꼬리까지 흘러가는 반투명 반짝임 웨이브 / 게임오버 화면을 오래 방치했다가 재시작하면 즉시 충돌사하던 버그 수정(프레임 공백 이후 틱 폭주 클램프) / 5번적(헌터) 체력 20 → 7로 밸런스 조정 |
| 2.6.0 | 2026-07-30 | **실시간 점수 시스템 추가** — 생존/아이템/처치 점수를 실시간으로 누적 표시, 길이·속도 최고치 보너스 반영, 리더보드 정렬 기준을 생존시간에서 점수로 전환(기존 기록은 자동 환산) / **독침·비늘파동 무기 시스템 추가** — 뱀 길이 8 이상에서 독침(단발 투사체), 3번적 5킬 달성 후 비늘파동(충전형 광역 공격) 잠금 해제 + 습득 시 안내 팝업, 일시정지(P/Esc) 기능 추가 / 하단 상태창 개편 — 타이틀/리더보드 화면과 자연스럽게 이어지는 병합 레이아웃, 1~3열에 제목(STATUS/KILL STACK/CINTAMANI) 표시, 1열(속도·길이·독침·비늘파동)과 3열(CINTAMANI, 2x2→1x4)을 아이콘+숫자 한 줄 배치로 통일(무기 스탯은 실제 획득 후에만 노출), 높이 100→80px로 축소 / 방향키를 빠르게 두 번 누르면 하나로 씹히던 버그, 게임오버 화면을 오래 방치했다가 재시작하면 즉시 충돌사하던 버그, 리더보드 순위표 목록이 캔버스 밖으로 넘치던 버그 수정 / 한글 입력기(IME) 활성 상태에서 P·T·L 등 키 입력이 안 먹던 문제 수정 / 포획존형 적(2·4번) 두 마리가 붙어 나와 포획 불가능해지던 문제 방지, 터렛·헌터 발사체가 다른 적과 부딪히면 고정 피해를 주도록 변경, 독침이 스페이스바를 누르고 있으면 연사되던 버그 수정 / 보라 먹이 성장량을 고정 3칸에서 뱀 길이 비례(3배)로 변경 / 적이 3마리를 넘으면 생성이 완전히 멈추던 것을 없애고, 초과 마리당 생성 간격만 2배씩 지수적으로 늘어나도록 변경(무제한 스폰 유지) / 무기 잠금 해제 팝업 글씨 크기 확대 |
